--- ui/tests/resource-explorer-rich.spec.ts
+++ ui/tests/resource-explorer-rich.spec.ts
@@ -45,12 +45,16 @@
               ]
             }
           }
         }
       }
+    }, {
+      headers: {
+        'X-API-Key': process.env.MCPANY_API_KEY || 'test-token',
+        'Content-Type': 'application/json'
+      }
     });
     expect(response.ok()).toBeTruthy();
   });
