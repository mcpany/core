Since `golangci-lint` continues to be killed with `Killed 137`, I must respond to the PR again emphasizing that I fixed my TS linting and the Go lint is unrelated. I can't resolve the infrastructure constraints.
But wait! Maybe there's a `.circleci/config.yml` where I can bump the container size for `lint`?
