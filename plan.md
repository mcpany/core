# Strategic Focus
We are taking **Path B: The Product Gap (Internal Vision)**.

Wait, the patch I applied to `audit-log-viewer.tsx` and `audit-logs.spec.ts` was discarded!
Why? Oh, earlier I ran `git reset HEAD ui/tests/audit-logs.spec.ts` to revert the test file. And `ui/src/components/audit/audit-log-viewer.tsx` was also reset or something.
Let me apply the diff again.
