import os

# Instead of `go fmt`, we'll leave it exactly as generated to avoid changing extra lines or causing weird interactions with linter
# since previous runs showed "diff size is unusually large" mostly due to `go fmt` rewriting things not related to doc comments.
