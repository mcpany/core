---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: mcpservers.mcp.any
spec:
  group: mcp.any
  names:
    kind: MCPServer
    listKind: MCPServerList
    plural: mcpservers
    singular: mcpserver
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MCPServer is the Schema for the mcpservers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MCPServerSpec defines the desired state of MCPServer
            properties:
              configMap:
                description: ConfigMap is the name of the ConfigMap containing config.yaml
                type: string
              image:
                description: Image is the container image to use
                type: string
              replicas:
                description: Replicas is the number of replicas for the server
                format: int32
                type: integer
              servicePort:
                description: ServicePort is the port to expose the service on. Defaults
                  to 8080.
                format: int32
                type: integer
              serviceType:
                description: ServiceType is the type of Kubernetes Service to expose
                  (ClusterIP, LoadBalancer, NodePort)
                type: string
            required:
            - configMap
            type: object
          status:
            description: MCPServerStatus defines the observed state of MCPServer
            properties:
              availableReplicas:
                description: AvailableReplicas is the number of available replicas
                format: int32
                type: integer
            required:
            - availableReplicas
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
