// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.1
// source: proto/examples/weather/v1/weather.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { grpc } from "@improbable-eng/grpc-web";
import { BrowserHeaders } from "browser-headers";
import Long from "long";

export const protobufPackage = "examples.weather.v1";

export interface GetWeatherRequest {
  location: string;
}

export interface GetWeatherResponse {
  weather: string;
}

function createBaseGetWeatherRequest(): GetWeatherRequest {
  return { location: "" };
}

export const GetWeatherRequest: MessageFns<GetWeatherRequest> = {
  encode(message: GetWeatherRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.location !== "") {
      writer.uint32(10).string(message.location);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetWeatherRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetWeatherRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.location = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetWeatherRequest {
    return { location: isSet(object.location) ? globalThis.String(object.location) : "" };
  },

  toJSON(message: GetWeatherRequest): unknown {
    const obj: any = {};
    if (message.location !== "") {
      obj.location = message.location;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetWeatherRequest>, I>>(base?: I): GetWeatherRequest {
    return GetWeatherRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetWeatherRequest>, I>>(object: I): GetWeatherRequest {
    const message = createBaseGetWeatherRequest();
    message.location = object.location ?? "";
    return message;
  },
};

function createBaseGetWeatherResponse(): GetWeatherResponse {
  return { weather: "" };
}

export const GetWeatherResponse: MessageFns<GetWeatherResponse> = {
  encode(message: GetWeatherResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.weather !== "") {
      writer.uint32(10).string(message.weather);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetWeatherResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetWeatherResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.weather = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetWeatherResponse {
    return { weather: isSet(object.weather) ? globalThis.String(object.weather) : "" };
  },

  toJSON(message: GetWeatherResponse): unknown {
    const obj: any = {};
    if (message.weather !== "") {
      obj.weather = message.weather;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetWeatherResponse>, I>>(base?: I): GetWeatherResponse {
    return GetWeatherResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetWeatherResponse>, I>>(object: I): GetWeatherResponse {
    const message = createBaseGetWeatherResponse();
    message.weather = object.weather ?? "";
    return message;
  },
};

export interface WeatherService {
  GetWeather(request: DeepPartial<GetWeatherRequest>, metadata?: grpc.Metadata): Promise<GetWeatherResponse>;
}

export class WeatherServiceClientImpl implements WeatherService {
  private readonly rpc: Rpc;

  constructor(rpc: Rpc) {
    this.rpc = rpc;
    this.GetWeather = this.GetWeather.bind(this);
  }

  GetWeather(request: DeepPartial<GetWeatherRequest>, metadata?: grpc.Metadata): Promise<GetWeatherResponse> {
    return this.rpc.unary(WeatherServiceGetWeatherDesc, GetWeatherRequest.fromPartial(request), metadata);
  }
}

export const WeatherServiceDesc = { serviceName: "examples.weather.v1.WeatherService" };

export const WeatherServiceGetWeatherDesc: UnaryMethodDefinitionish = {
  methodName: "GetWeather",
  service: WeatherServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return GetWeatherRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = GetWeatherResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

interface UnaryMethodDefinitionishR extends grpc.UnaryMethodDefinition<any, any> {
  requestStream: any;
  responseStream: any;
}

type UnaryMethodDefinitionish = UnaryMethodDefinitionishR;

interface Rpc {
  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any>;
}

export class GrpcWebImpl {
  private host: string;
  private options: {
    transport?: grpc.TransportFactory;

    debug?: boolean;
    metadata?: grpc.Metadata;
    upStreamRetryCodes?: number[];
  };

  constructor(
    host: string,
    options: {
      transport?: grpc.TransportFactory;

      debug?: boolean;
      metadata?: grpc.Metadata;
      upStreamRetryCodes?: number[];
    },
  ) {
    this.host = host;
    this.options = options;
  }

  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    _request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any> {
    const request = { ..._request, ...methodDesc.requestType };
    const maybeCombinedMetadata = metadata && this.options.metadata
      ? new BrowserHeaders({ ...this.options?.metadata.headersMap, ...metadata?.headersMap })
      : metadata ?? this.options.metadata;
    return new Promise((resolve, reject) => {
      grpc.unary(methodDesc, {
        request,
        host: this.host,
        metadata: maybeCombinedMetadata ?? {},
        ...(this.options.transport !== undefined ? { transport: this.options.transport } : {}),
        debug: this.options.debug ?? false,
        onEnd: function (response) {
          if (response.status === grpc.Code.OK) {
            resolve(response.message!.toObject());
          } else {
            const err = new GrpcWebError(response.statusMessage, response.status, response.trailers);
            reject(err);
          }
        },
      });
    });
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export class GrpcWebError extends globalThis.Error {
  constructor(message: string, public code: grpc.Code, public metadata: grpc.Metadata) {
    super(message);
  }
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
