// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.1
// source: proto/mcp_router/v1/mcp_router.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { grpc } from "@improbable-eng/grpc-web";
import { BrowserHeaders } from "browser-headers";
import Long from "long";
import { Struct } from "../../google/protobuf/struct";

export const protobufPackage = "mcpany.mcp_router.v1";

/** Represents an MCP Tool exposed by the aggregator */
export interface Tool {
  /** Unique name, e.g., "serviceName/methodName" or a hash */
  name: string;
  /** User-friendly name */
  displayName: string;
  /** Description from annotation */
  description: string;
  /** The ID of the registered user service that provides this tool */
  serviceId: string;
  /** Fully qualified name of the gRPC method, e.g., /pkg.UserService/Method */
  underlyingMethodFqn: string;
  /** Fully qualified name of the underlying method's request message */
  requestTypeFqn: string;
  /** Fully qualified name of the underlying method's response message */
  responseTypeFqn: string;
  inputSchema?: { [key: string]: any } | undefined;
  outputSchema?: { [key: string]: any } | undefined;
  annotations?: ToolAnnotations | undefined;
  isStream: boolean;
  tags: string[];
  profiles: string[];
  integrity?: ToolIntegrity | undefined;
}

export interface ToolIntegrity {
  hash: string;
  /** e.g. "sha256" */
  algorithm: string;
}

export interface ToolAnnotations {
  title: string;
  /**
   * If true, the tool does not modify its environment.
   * Default: false
   */
  readOnlyHint: boolean;
  /**
   * If true, the tool may perform destructive updates to its environment. If
   * false, the tool performs only additive updates.
   * (This property is meaningful only when ReadOnlyHint == false.)
   * Default: true
   */
  destructiveHint: boolean;
  /**
   * If true, calling the tool repeatedly with the same arguments will have no
   * additional effect on the its environment.
   * (This property is meaningful only when ReadOnlyHint == false.)
   * Default: false
   */
  idempotentHint: boolean;
  /**
   * If true, this tool may interact with an "open world" of external entities. If
   * false, the tool's domain of interaction is closed. For example, the world of
   * a web search tool is open, whereas that of a memory tool is not.
   * Default: true
   */
  openWorldHint: boolean;
  inputSchema?: { [key: string]: any } | undefined;
  outputSchema?: { [key: string]: any } | undefined;
}

/** Future: filtering options */
export interface ListToolsRequest {
}

export interface ListToolsResponse {
  tools: Tool[];
}

export interface CallToolRequest {
  /** ID of the tool to execute */
  toolId: string;
  /** Actual inputs for the tool, as a JSON-like object */
  inputs?: { [key: string]: any } | undefined;
}

export interface CallToolResponse {
  /** Actual outputs from the tool, as a JSON-like object */
  outputs?: { [key: string]: any } | undefined;
}

/** A Prompt is a pre-defined template for interacting with a language model. */
export interface Prompt {
  name: string;
  title: string;
  description: string;
  arguments: PromptArgument[];
}

export interface PromptArgument {
  name: string;
  description: string;
  required: boolean;
}

export interface ListPromptsRequest {
  cursor: string;
}

export interface ListPromptsResponse {
  prompts: Prompt[];
  nextCursor: string;
}

export interface GetPromptRequest {
  name: string;
  arguments?: { [key: string]: any } | undefined;
}

export interface GetPromptResponse {
  description: string;
  messages: PromptMessage[];
}

export interface PromptMessage {
  role: PromptMessage_Role;
  text?: TextContent | undefined;
  image?: ImageContent | undefined;
  audio?: AudioContent | undefined;
  resource?: ResourceContent | undefined;
}

export enum PromptMessage_Role {
  USER = 0,
  ASSISTANT = 1,
  UNRECOGNIZED = -1,
}

export function promptMessage_RoleFromJSON(object: any): PromptMessage_Role {
  switch (object) {
    case 0:
    case "USER":
      return PromptMessage_Role.USER;
    case 1:
    case "ASSISTANT":
      return PromptMessage_Role.ASSISTANT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PromptMessage_Role.UNRECOGNIZED;
  }
}

export function promptMessage_RoleToJSON(object: PromptMessage_Role): string {
  switch (object) {
    case PromptMessage_Role.USER:
      return "USER";
    case PromptMessage_Role.ASSISTANT:
      return "ASSISTANT";
    case PromptMessage_Role.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface TextContent {
  text: string;
}

export interface ImageContent {
  data: string;
  mimeType: string;
}

export interface AudioContent {
  data: string;
  mimeType: string;
}

export interface ResourceContent {
  uri: string;
  mimeType: string;
  text?: string | undefined;
  blob?: Uint8Array | undefined;
}

/** A Resource is a piece of data that can be used by tools or prompts. */
export interface Resource {
  id: string;
  name: string;
  description: string;
  /** e.g., "text", "image", "url" */
  type: string;
  content: Uint8Array;
}

export interface ListResourcesRequest {
}

export interface ListResourcesResponse {
  resources: Resource[];
}

function createBaseTool(): Tool {
  return {
    name: "",
    displayName: "",
    description: "",
    serviceId: "",
    underlyingMethodFqn: "",
    requestTypeFqn: "",
    responseTypeFqn: "",
    inputSchema: undefined,
    outputSchema: undefined,
    annotations: undefined,
    isStream: false,
    tags: [],
    profiles: [],
    integrity: undefined,
  };
}

export const Tool: MessageFns<Tool> = {
  encode(message: Tool, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.displayName !== "") {
      writer.uint32(18).string(message.displayName);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.serviceId !== "") {
      writer.uint32(34).string(message.serviceId);
    }
    if (message.underlyingMethodFqn !== "") {
      writer.uint32(42).string(message.underlyingMethodFqn);
    }
    if (message.requestTypeFqn !== "") {
      writer.uint32(50).string(message.requestTypeFqn);
    }
    if (message.responseTypeFqn !== "") {
      writer.uint32(58).string(message.responseTypeFqn);
    }
    if (message.inputSchema !== undefined) {
      Struct.encode(Struct.wrap(message.inputSchema), writer.uint32(66).fork()).join();
    }
    if (message.outputSchema !== undefined) {
      Struct.encode(Struct.wrap(message.outputSchema), writer.uint32(74).fork()).join();
    }
    if (message.annotations !== undefined) {
      ToolAnnotations.encode(message.annotations, writer.uint32(90).fork()).join();
    }
    if (message.isStream !== false) {
      writer.uint32(96).bool(message.isStream);
    }
    for (const v of message.tags) {
      writer.uint32(106).string(v!);
    }
    for (const v of message.profiles) {
      writer.uint32(114).string(v!);
    }
    if (message.integrity !== undefined) {
      ToolIntegrity.encode(message.integrity, writer.uint32(122).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Tool {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTool();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.serviceId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.underlyingMethodFqn = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.requestTypeFqn = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.responseTypeFqn = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.inputSchema = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.outputSchema = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.annotations = ToolAnnotations.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.isStream = reader.bool();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.tags.push(reader.string());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.profiles.push(reader.string());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.integrity = ToolIntegrity.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Tool {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      serviceId: isSet(object.serviceId)
        ? globalThis.String(object.serviceId)
        : isSet(object.service_id)
        ? globalThis.String(object.service_id)
        : "",
      underlyingMethodFqn: isSet(object.underlyingMethodFqn)
        ? globalThis.String(object.underlyingMethodFqn)
        : isSet(object.underlying_method_fqn)
        ? globalThis.String(object.underlying_method_fqn)
        : "",
      requestTypeFqn: isSet(object.requestTypeFqn)
        ? globalThis.String(object.requestTypeFqn)
        : isSet(object.request_type_fqn)
        ? globalThis.String(object.request_type_fqn)
        : "",
      responseTypeFqn: isSet(object.responseTypeFqn)
        ? globalThis.String(object.responseTypeFqn)
        : isSet(object.response_type_fqn)
        ? globalThis.String(object.response_type_fqn)
        : "",
      inputSchema: isObject(object.inputSchema)
        ? object.inputSchema
        : isObject(object.input_schema)
        ? object.input_schema
        : undefined,
      outputSchema: isObject(object.outputSchema)
        ? object.outputSchema
        : isObject(object.output_schema)
        ? object.output_schema
        : undefined,
      annotations: isSet(object.annotations) ? ToolAnnotations.fromJSON(object.annotations) : undefined,
      isStream: isSet(object.isStream)
        ? globalThis.Boolean(object.isStream)
        : isSet(object.is_stream)
        ? globalThis.Boolean(object.is_stream)
        : false,
      tags: globalThis.Array.isArray(object?.tags)
        ? object.tags.map((e: any) => globalThis.String(e))
        : [],
      profiles: globalThis.Array.isArray(object?.profiles)
        ? object.profiles.map((e: any) => globalThis.String(e))
        : [],
      integrity: isSet(object.integrity) ? ToolIntegrity.fromJSON(object.integrity) : undefined,
    };
  },

  toJSON(message: Tool): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.serviceId !== "") {
      obj.serviceId = message.serviceId;
    }
    if (message.underlyingMethodFqn !== "") {
      obj.underlyingMethodFqn = message.underlyingMethodFqn;
    }
    if (message.requestTypeFqn !== "") {
      obj.requestTypeFqn = message.requestTypeFqn;
    }
    if (message.responseTypeFqn !== "") {
      obj.responseTypeFqn = message.responseTypeFqn;
    }
    if (message.inputSchema !== undefined) {
      obj.inputSchema = message.inputSchema;
    }
    if (message.outputSchema !== undefined) {
      obj.outputSchema = message.outputSchema;
    }
    if (message.annotations !== undefined) {
      obj.annotations = ToolAnnotations.toJSON(message.annotations);
    }
    if (message.isStream !== false) {
      obj.isStream = message.isStream;
    }
    if (message.tags?.length) {
      obj.tags = message.tags;
    }
    if (message.profiles?.length) {
      obj.profiles = message.profiles;
    }
    if (message.integrity !== undefined) {
      obj.integrity = ToolIntegrity.toJSON(message.integrity);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Tool>, I>>(base?: I): Tool {
    return Tool.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Tool>, I>>(object: I): Tool {
    const message = createBaseTool();
    message.name = object.name ?? "";
    message.displayName = object.displayName ?? "";
    message.description = object.description ?? "";
    message.serviceId = object.serviceId ?? "";
    message.underlyingMethodFqn = object.underlyingMethodFqn ?? "";
    message.requestTypeFqn = object.requestTypeFqn ?? "";
    message.responseTypeFqn = object.responseTypeFqn ?? "";
    message.inputSchema = object.inputSchema ?? undefined;
    message.outputSchema = object.outputSchema ?? undefined;
    message.annotations = (object.annotations !== undefined && object.annotations !== null)
      ? ToolAnnotations.fromPartial(object.annotations)
      : undefined;
    message.isStream = object.isStream ?? false;
    message.tags = object.tags?.map((e) => e) || [];
    message.profiles = object.profiles?.map((e) => e) || [];
    message.integrity = (object.integrity !== undefined && object.integrity !== null)
      ? ToolIntegrity.fromPartial(object.integrity)
      : undefined;
    return message;
  },
};

function createBaseToolIntegrity(): ToolIntegrity {
  return { hash: "", algorithm: "" };
}

export const ToolIntegrity: MessageFns<ToolIntegrity> = {
  encode(message: ToolIntegrity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hash !== "") {
      writer.uint32(10).string(message.hash);
    }
    if (message.algorithm !== "") {
      writer.uint32(18).string(message.algorithm);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolIntegrity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolIntegrity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hash = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.algorithm = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolIntegrity {
    return {
      hash: isSet(object.hash) ? globalThis.String(object.hash) : "",
      algorithm: isSet(object.algorithm) ? globalThis.String(object.algorithm) : "",
    };
  },

  toJSON(message: ToolIntegrity): unknown {
    const obj: any = {};
    if (message.hash !== "") {
      obj.hash = message.hash;
    }
    if (message.algorithm !== "") {
      obj.algorithm = message.algorithm;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolIntegrity>, I>>(base?: I): ToolIntegrity {
    return ToolIntegrity.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolIntegrity>, I>>(object: I): ToolIntegrity {
    const message = createBaseToolIntegrity();
    message.hash = object.hash ?? "";
    message.algorithm = object.algorithm ?? "";
    return message;
  },
};

function createBaseToolAnnotations(): ToolAnnotations {
  return {
    title: "",
    readOnlyHint: false,
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    inputSchema: undefined,
    outputSchema: undefined,
  };
}

export const ToolAnnotations: MessageFns<ToolAnnotations> = {
  encode(message: ToolAnnotations, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.readOnlyHint !== false) {
      writer.uint32(16).bool(message.readOnlyHint);
    }
    if (message.destructiveHint !== false) {
      writer.uint32(24).bool(message.destructiveHint);
    }
    if (message.idempotentHint !== false) {
      writer.uint32(32).bool(message.idempotentHint);
    }
    if (message.openWorldHint !== false) {
      writer.uint32(40).bool(message.openWorldHint);
    }
    if (message.inputSchema !== undefined) {
      Struct.encode(Struct.wrap(message.inputSchema), writer.uint32(50).fork()).join();
    }
    if (message.outputSchema !== undefined) {
      Struct.encode(Struct.wrap(message.outputSchema), writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolAnnotations {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolAnnotations();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.readOnlyHint = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.destructiveHint = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.idempotentHint = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.openWorldHint = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.inputSchema = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.outputSchema = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolAnnotations {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      readOnlyHint: isSet(object.readOnlyHint)
        ? globalThis.Boolean(object.readOnlyHint)
        : isSet(object.read_only_hint)
        ? globalThis.Boolean(object.read_only_hint)
        : false,
      destructiveHint: isSet(object.destructiveHint)
        ? globalThis.Boolean(object.destructiveHint)
        : isSet(object.destructive_hint)
        ? globalThis.Boolean(object.destructive_hint)
        : false,
      idempotentHint: isSet(object.idempotentHint)
        ? globalThis.Boolean(object.idempotentHint)
        : isSet(object.idempotent_hint)
        ? globalThis.Boolean(object.idempotent_hint)
        : false,
      openWorldHint: isSet(object.openWorldHint)
        ? globalThis.Boolean(object.openWorldHint)
        : isSet(object.open_world_hint)
        ? globalThis.Boolean(object.open_world_hint)
        : false,
      inputSchema: isObject(object.inputSchema)
        ? object.inputSchema
        : isObject(object.input_schema)
        ? object.input_schema
        : undefined,
      outputSchema: isObject(object.outputSchema)
        ? object.outputSchema
        : isObject(object.output_schema)
        ? object.output_schema
        : undefined,
    };
  },

  toJSON(message: ToolAnnotations): unknown {
    const obj: any = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.readOnlyHint !== false) {
      obj.readOnlyHint = message.readOnlyHint;
    }
    if (message.destructiveHint !== false) {
      obj.destructiveHint = message.destructiveHint;
    }
    if (message.idempotentHint !== false) {
      obj.idempotentHint = message.idempotentHint;
    }
    if (message.openWorldHint !== false) {
      obj.openWorldHint = message.openWorldHint;
    }
    if (message.inputSchema !== undefined) {
      obj.inputSchema = message.inputSchema;
    }
    if (message.outputSchema !== undefined) {
      obj.outputSchema = message.outputSchema;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolAnnotations>, I>>(base?: I): ToolAnnotations {
    return ToolAnnotations.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolAnnotations>, I>>(object: I): ToolAnnotations {
    const message = createBaseToolAnnotations();
    message.title = object.title ?? "";
    message.readOnlyHint = object.readOnlyHint ?? false;
    message.destructiveHint = object.destructiveHint ?? false;
    message.idempotentHint = object.idempotentHint ?? false;
    message.openWorldHint = object.openWorldHint ?? false;
    message.inputSchema = object.inputSchema ?? undefined;
    message.outputSchema = object.outputSchema ?? undefined;
    return message;
  },
};

function createBaseListToolsRequest(): ListToolsRequest {
  return {};
}

export const ListToolsRequest: MessageFns<ListToolsRequest> = {
  encode(_: ListToolsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListToolsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListToolsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListToolsRequest {
    return {};
  },

  toJSON(_: ListToolsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListToolsRequest>, I>>(base?: I): ListToolsRequest {
    return ListToolsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListToolsRequest>, I>>(_: I): ListToolsRequest {
    const message = createBaseListToolsRequest();
    return message;
  },
};

function createBaseListToolsResponse(): ListToolsResponse {
  return { tools: [] };
}

export const ListToolsResponse: MessageFns<ListToolsResponse> = {
  encode(message: ListToolsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tools) {
      Tool.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListToolsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListToolsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tools.push(Tool.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListToolsResponse {
    return { tools: globalThis.Array.isArray(object?.tools) ? object.tools.map((e: any) => Tool.fromJSON(e)) : [] };
  },

  toJSON(message: ListToolsResponse): unknown {
    const obj: any = {};
    if (message.tools?.length) {
      obj.tools = message.tools.map((e) => Tool.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListToolsResponse>, I>>(base?: I): ListToolsResponse {
    return ListToolsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListToolsResponse>, I>>(object: I): ListToolsResponse {
    const message = createBaseListToolsResponse();
    message.tools = object.tools?.map((e) => Tool.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCallToolRequest(): CallToolRequest {
  return { toolId: "", inputs: undefined };
}

export const CallToolRequest: MessageFns<CallToolRequest> = {
  encode(message: CallToolRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.toolId !== "") {
      writer.uint32(10).string(message.toolId);
    }
    if (message.inputs !== undefined) {
      Struct.encode(Struct.wrap(message.inputs), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CallToolRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCallToolRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.toolId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.inputs = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CallToolRequest {
    return {
      toolId: isSet(object.toolId)
        ? globalThis.String(object.toolId)
        : isSet(object.tool_id)
        ? globalThis.String(object.tool_id)
        : "",
      inputs: isObject(object.inputs) ? object.inputs : undefined,
    };
  },

  toJSON(message: CallToolRequest): unknown {
    const obj: any = {};
    if (message.toolId !== "") {
      obj.toolId = message.toolId;
    }
    if (message.inputs !== undefined) {
      obj.inputs = message.inputs;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CallToolRequest>, I>>(base?: I): CallToolRequest {
    return CallToolRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CallToolRequest>, I>>(object: I): CallToolRequest {
    const message = createBaseCallToolRequest();
    message.toolId = object.toolId ?? "";
    message.inputs = object.inputs ?? undefined;
    return message;
  },
};

function createBaseCallToolResponse(): CallToolResponse {
  return { outputs: undefined };
}

export const CallToolResponse: MessageFns<CallToolResponse> = {
  encode(message: CallToolResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.outputs !== undefined) {
      Struct.encode(Struct.wrap(message.outputs), writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CallToolResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCallToolResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.outputs = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CallToolResponse {
    return { outputs: isObject(object.outputs) ? object.outputs : undefined };
  },

  toJSON(message: CallToolResponse): unknown {
    const obj: any = {};
    if (message.outputs !== undefined) {
      obj.outputs = message.outputs;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CallToolResponse>, I>>(base?: I): CallToolResponse {
    return CallToolResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CallToolResponse>, I>>(object: I): CallToolResponse {
    const message = createBaseCallToolResponse();
    message.outputs = object.outputs ?? undefined;
    return message;
  },
};

function createBasePrompt(): Prompt {
  return { name: "", title: "", description: "", arguments: [] };
}

export const Prompt: MessageFns<Prompt> = {
  encode(message: Prompt, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    for (const v of message.arguments) {
      PromptArgument.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Prompt {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrompt();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.arguments.push(PromptArgument.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Prompt {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      arguments: globalThis.Array.isArray(object?.arguments)
        ? object.arguments.map((e: any) => PromptArgument.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Prompt): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.arguments?.length) {
      obj.arguments = message.arguments.map((e) => PromptArgument.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Prompt>, I>>(base?: I): Prompt {
    return Prompt.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Prompt>, I>>(object: I): Prompt {
    const message = createBasePrompt();
    message.name = object.name ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.arguments = object.arguments?.map((e) => PromptArgument.fromPartial(e)) || [];
    return message;
  },
};

function createBasePromptArgument(): PromptArgument {
  return { name: "", description: "", required: false };
}

export const PromptArgument: MessageFns<PromptArgument> = {
  encode(message: PromptArgument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.required !== false) {
      writer.uint32(24).bool(message.required);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PromptArgument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePromptArgument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.required = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PromptArgument {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      required: isSet(object.required) ? globalThis.Boolean(object.required) : false,
    };
  },

  toJSON(message: PromptArgument): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.required !== false) {
      obj.required = message.required;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PromptArgument>, I>>(base?: I): PromptArgument {
    return PromptArgument.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PromptArgument>, I>>(object: I): PromptArgument {
    const message = createBasePromptArgument();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.required = object.required ?? false;
    return message;
  },
};

function createBaseListPromptsRequest(): ListPromptsRequest {
  return { cursor: "" };
}

export const ListPromptsRequest: MessageFns<ListPromptsRequest> = {
  encode(message: ListPromptsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cursor !== "") {
      writer.uint32(10).string(message.cursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPromptsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPromptsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.cursor = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPromptsRequest {
    return { cursor: isSet(object.cursor) ? globalThis.String(object.cursor) : "" };
  },

  toJSON(message: ListPromptsRequest): unknown {
    const obj: any = {};
    if (message.cursor !== "") {
      obj.cursor = message.cursor;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListPromptsRequest>, I>>(base?: I): ListPromptsRequest {
    return ListPromptsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListPromptsRequest>, I>>(object: I): ListPromptsRequest {
    const message = createBaseListPromptsRequest();
    message.cursor = object.cursor ?? "";
    return message;
  },
};

function createBaseListPromptsResponse(): ListPromptsResponse {
  return { prompts: [], nextCursor: "" };
}

export const ListPromptsResponse: MessageFns<ListPromptsResponse> = {
  encode(message: ListPromptsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.prompts) {
      Prompt.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.nextCursor !== "") {
      writer.uint32(18).string(message.nextCursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPromptsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPromptsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prompts.push(Prompt.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nextCursor = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPromptsResponse {
    return {
      prompts: globalThis.Array.isArray(object?.prompts) ? object.prompts.map((e: any) => Prompt.fromJSON(e)) : [],
      nextCursor: isSet(object.nextCursor)
        ? globalThis.String(object.nextCursor)
        : isSet(object.next_cursor)
        ? globalThis.String(object.next_cursor)
        : "",
    };
  },

  toJSON(message: ListPromptsResponse): unknown {
    const obj: any = {};
    if (message.prompts?.length) {
      obj.prompts = message.prompts.map((e) => Prompt.toJSON(e));
    }
    if (message.nextCursor !== "") {
      obj.nextCursor = message.nextCursor;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListPromptsResponse>, I>>(base?: I): ListPromptsResponse {
    return ListPromptsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListPromptsResponse>, I>>(object: I): ListPromptsResponse {
    const message = createBaseListPromptsResponse();
    message.prompts = object.prompts?.map((e) => Prompt.fromPartial(e)) || [];
    message.nextCursor = object.nextCursor ?? "";
    return message;
  },
};

function createBaseGetPromptRequest(): GetPromptRequest {
  return { name: "", arguments: undefined };
}

export const GetPromptRequest: MessageFns<GetPromptRequest> = {
  encode(message: GetPromptRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.arguments !== undefined) {
      Struct.encode(Struct.wrap(message.arguments), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPromptRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPromptRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.arguments = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPromptRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      arguments: isObject(object.arguments) ? object.arguments : undefined,
    };
  },

  toJSON(message: GetPromptRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.arguments !== undefined) {
      obj.arguments = message.arguments;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPromptRequest>, I>>(base?: I): GetPromptRequest {
    return GetPromptRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPromptRequest>, I>>(object: I): GetPromptRequest {
    const message = createBaseGetPromptRequest();
    message.name = object.name ?? "";
    message.arguments = object.arguments ?? undefined;
    return message;
  },
};

function createBaseGetPromptResponse(): GetPromptResponse {
  return { description: "", messages: [] };
}

export const GetPromptResponse: MessageFns<GetPromptResponse> = {
  encode(message: GetPromptResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.description !== "") {
      writer.uint32(10).string(message.description);
    }
    for (const v of message.messages) {
      PromptMessage.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPromptResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPromptResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.messages.push(PromptMessage.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPromptResponse {
    return {
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => PromptMessage.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetPromptResponse): unknown {
    const obj: any = {};
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => PromptMessage.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPromptResponse>, I>>(base?: I): GetPromptResponse {
    return GetPromptResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPromptResponse>, I>>(object: I): GetPromptResponse {
    const message = createBaseGetPromptResponse();
    message.description = object.description ?? "";
    message.messages = object.messages?.map((e) => PromptMessage.fromPartial(e)) || [];
    return message;
  },
};

function createBasePromptMessage(): PromptMessage {
  return { role: 0, text: undefined, image: undefined, audio: undefined, resource: undefined };
}

export const PromptMessage: MessageFns<PromptMessage> = {
  encode(message: PromptMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.role !== 0) {
      writer.uint32(8).int32(message.role);
    }
    if (message.text !== undefined) {
      TextContent.encode(message.text, writer.uint32(18).fork()).join();
    }
    if (message.image !== undefined) {
      ImageContent.encode(message.image, writer.uint32(26).fork()).join();
    }
    if (message.audio !== undefined) {
      AudioContent.encode(message.audio, writer.uint32(34).fork()).join();
    }
    if (message.resource !== undefined) {
      ResourceContent.encode(message.resource, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PromptMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePromptMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.text = TextContent.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.image = ImageContent.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.audio = AudioContent.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.resource = ResourceContent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PromptMessage {
    return {
      role: isSet(object.role) ? promptMessage_RoleFromJSON(object.role) : 0,
      text: isSet(object.text) ? TextContent.fromJSON(object.text) : undefined,
      image: isSet(object.image) ? ImageContent.fromJSON(object.image) : undefined,
      audio: isSet(object.audio) ? AudioContent.fromJSON(object.audio) : undefined,
      resource: isSet(object.resource) ? ResourceContent.fromJSON(object.resource) : undefined,
    };
  },

  toJSON(message: PromptMessage): unknown {
    const obj: any = {};
    if (message.role !== 0) {
      obj.role = promptMessage_RoleToJSON(message.role);
    }
    if (message.text !== undefined) {
      obj.text = TextContent.toJSON(message.text);
    }
    if (message.image !== undefined) {
      obj.image = ImageContent.toJSON(message.image);
    }
    if (message.audio !== undefined) {
      obj.audio = AudioContent.toJSON(message.audio);
    }
    if (message.resource !== undefined) {
      obj.resource = ResourceContent.toJSON(message.resource);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PromptMessage>, I>>(base?: I): PromptMessage {
    return PromptMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PromptMessage>, I>>(object: I): PromptMessage {
    const message = createBasePromptMessage();
    message.role = object.role ?? 0;
    message.text = (object.text !== undefined && object.text !== null)
      ? TextContent.fromPartial(object.text)
      : undefined;
    message.image = (object.image !== undefined && object.image !== null)
      ? ImageContent.fromPartial(object.image)
      : undefined;
    message.audio = (object.audio !== undefined && object.audio !== null)
      ? AudioContent.fromPartial(object.audio)
      : undefined;
    message.resource = (object.resource !== undefined && object.resource !== null)
      ? ResourceContent.fromPartial(object.resource)
      : undefined;
    return message;
  },
};

function createBaseTextContent(): TextContent {
  return { text: "" };
}

export const TextContent: MessageFns<TextContent> = {
  encode(message: TextContent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TextContent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTextContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TextContent {
    return { text: isSet(object.text) ? globalThis.String(object.text) : "" };
  },

  toJSON(message: TextContent): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TextContent>, I>>(base?: I): TextContent {
    return TextContent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TextContent>, I>>(object: I): TextContent {
    const message = createBaseTextContent();
    message.text = object.text ?? "";
    return message;
  },
};

function createBaseImageContent(): ImageContent {
  return { data: "", mimeType: "" };
}

export const ImageContent: MessageFns<ImageContent> = {
  encode(message: ImageContent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== "") {
      writer.uint32(10).string(message.data);
    }
    if (message.mimeType !== "") {
      writer.uint32(18).string(message.mimeType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageContent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageContent {
    return {
      data: isSet(object.data) ? globalThis.String(object.data) : "",
      mimeType: isSet(object.mimeType)
        ? globalThis.String(object.mimeType)
        : isSet(object.mime_type)
        ? globalThis.String(object.mime_type)
        : "",
    };
  },

  toJSON(message: ImageContent): unknown {
    const obj: any = {};
    if (message.data !== "") {
      obj.data = message.data;
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageContent>, I>>(base?: I): ImageContent {
    return ImageContent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageContent>, I>>(object: I): ImageContent {
    const message = createBaseImageContent();
    message.data = object.data ?? "";
    message.mimeType = object.mimeType ?? "";
    return message;
  },
};

function createBaseAudioContent(): AudioContent {
  return { data: "", mimeType: "" };
}

export const AudioContent: MessageFns<AudioContent> = {
  encode(message: AudioContent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== "") {
      writer.uint32(10).string(message.data);
    }
    if (message.mimeType !== "") {
      writer.uint32(18).string(message.mimeType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AudioContent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAudioContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AudioContent {
    return {
      data: isSet(object.data) ? globalThis.String(object.data) : "",
      mimeType: isSet(object.mimeType)
        ? globalThis.String(object.mimeType)
        : isSet(object.mime_type)
        ? globalThis.String(object.mime_type)
        : "",
    };
  },

  toJSON(message: AudioContent): unknown {
    const obj: any = {};
    if (message.data !== "") {
      obj.data = message.data;
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AudioContent>, I>>(base?: I): AudioContent {
    return AudioContent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AudioContent>, I>>(object: I): AudioContent {
    const message = createBaseAudioContent();
    message.data = object.data ?? "";
    message.mimeType = object.mimeType ?? "";
    return message;
  },
};

function createBaseResourceContent(): ResourceContent {
  return { uri: "", mimeType: "", text: undefined, blob: undefined };
}

export const ResourceContent: MessageFns<ResourceContent> = {
  encode(message: ResourceContent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uri !== "") {
      writer.uint32(10).string(message.uri);
    }
    if (message.mimeType !== "") {
      writer.uint32(18).string(message.mimeType);
    }
    if (message.text !== undefined) {
      writer.uint32(26).string(message.text);
    }
    if (message.blob !== undefined) {
      writer.uint32(34).bytes(message.blob);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResourceContent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResourceContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uri = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.blob = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResourceContent {
    return {
      uri: isSet(object.uri) ? globalThis.String(object.uri) : "",
      mimeType: isSet(object.mimeType)
        ? globalThis.String(object.mimeType)
        : isSet(object.mime_type)
        ? globalThis.String(object.mime_type)
        : "",
      text: isSet(object.text) ? globalThis.String(object.text) : undefined,
      blob: isSet(object.blob) ? bytesFromBase64(object.blob) : undefined,
    };
  },

  toJSON(message: ResourceContent): unknown {
    const obj: any = {};
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    if (message.text !== undefined) {
      obj.text = message.text;
    }
    if (message.blob !== undefined) {
      obj.blob = base64FromBytes(message.blob);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResourceContent>, I>>(base?: I): ResourceContent {
    return ResourceContent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResourceContent>, I>>(object: I): ResourceContent {
    const message = createBaseResourceContent();
    message.uri = object.uri ?? "";
    message.mimeType = object.mimeType ?? "";
    message.text = object.text ?? undefined;
    message.blob = object.blob ?? undefined;
    return message;
  },
};

function createBaseResource(): Resource {
  return { id: "", name: "", description: "", type: "", content: new Uint8Array(0) };
}

export const Resource: MessageFns<Resource> = {
  encode(message: Resource, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.type !== "") {
      writer.uint32(34).string(message.type);
    }
    if (message.content.length !== 0) {
      writer.uint32(42).bytes(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Resource {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResource();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.content = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Resource {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      content: isSet(object.content) ? bytesFromBase64(object.content) : new Uint8Array(0),
    };
  },

  toJSON(message: Resource): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.content.length !== 0) {
      obj.content = base64FromBytes(message.content);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Resource>, I>>(base?: I): Resource {
    return Resource.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Resource>, I>>(object: I): Resource {
    const message = createBaseResource();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.type = object.type ?? "";
    message.content = object.content ?? new Uint8Array(0);
    return message;
  },
};

function createBaseListResourcesRequest(): ListResourcesRequest {
  return {};
}

export const ListResourcesRequest: MessageFns<ListResourcesRequest> = {
  encode(_: ListResourcesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListResourcesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListResourcesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListResourcesRequest {
    return {};
  },

  toJSON(_: ListResourcesRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListResourcesRequest>, I>>(base?: I): ListResourcesRequest {
    return ListResourcesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListResourcesRequest>, I>>(_: I): ListResourcesRequest {
    const message = createBaseListResourcesRequest();
    return message;
  },
};

function createBaseListResourcesResponse(): ListResourcesResponse {
  return { resources: [] };
}

export const ListResourcesResponse: MessageFns<ListResourcesResponse> = {
  encode(message: ListResourcesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.resources) {
      Resource.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListResourcesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListResourcesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.resources.push(Resource.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListResourcesResponse {
    return {
      resources: globalThis.Array.isArray(object?.resources)
        ? object.resources.map((e: any) => Resource.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListResourcesResponse): unknown {
    const obj: any = {};
    if (message.resources?.length) {
      obj.resources = message.resources.map((e) => Resource.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListResourcesResponse>, I>>(base?: I): ListResourcesResponse {
    return ListResourcesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListResourcesResponse>, I>>(object: I): ListResourcesResponse {
    const message = createBaseListResourcesResponse();
    message.resources = object.resources?.map((e) => Resource.fromPartial(e)) || [];
    return message;
  },
};

export interface McpRouter {
  ListTools(request: DeepPartial<ListToolsRequest>, metadata?: grpc.Metadata): Promise<ListToolsResponse>;
  CallTool(request: DeepPartial<CallToolRequest>, metadata?: grpc.Metadata): Promise<CallToolResponse>;
  ListPrompts(request: DeepPartial<ListPromptsRequest>, metadata?: grpc.Metadata): Promise<ListPromptsResponse>;
  GetPrompt(request: DeepPartial<GetPromptRequest>, metadata?: grpc.Metadata): Promise<GetPromptResponse>;
  ListResources(request: DeepPartial<ListResourcesRequest>, metadata?: grpc.Metadata): Promise<ListResourcesResponse>;
}

export class McpRouterClientImpl implements McpRouter {
  private readonly rpc: Rpc;

  constructor(rpc: Rpc) {
    this.rpc = rpc;
    this.ListTools = this.ListTools.bind(this);
    this.CallTool = this.CallTool.bind(this);
    this.ListPrompts = this.ListPrompts.bind(this);
    this.GetPrompt = this.GetPrompt.bind(this);
    this.ListResources = this.ListResources.bind(this);
  }

  ListTools(request: DeepPartial<ListToolsRequest>, metadata?: grpc.Metadata): Promise<ListToolsResponse> {
    return this.rpc.unary(McpRouterListToolsDesc, ListToolsRequest.fromPartial(request), metadata);
  }

  CallTool(request: DeepPartial<CallToolRequest>, metadata?: grpc.Metadata): Promise<CallToolResponse> {
    return this.rpc.unary(McpRouterCallToolDesc, CallToolRequest.fromPartial(request), metadata);
  }

  ListPrompts(request: DeepPartial<ListPromptsRequest>, metadata?: grpc.Metadata): Promise<ListPromptsResponse> {
    return this.rpc.unary(McpRouterListPromptsDesc, ListPromptsRequest.fromPartial(request), metadata);
  }

  GetPrompt(request: DeepPartial<GetPromptRequest>, metadata?: grpc.Metadata): Promise<GetPromptResponse> {
    return this.rpc.unary(McpRouterGetPromptDesc, GetPromptRequest.fromPartial(request), metadata);
  }

  ListResources(request: DeepPartial<ListResourcesRequest>, metadata?: grpc.Metadata): Promise<ListResourcesResponse> {
    return this.rpc.unary(McpRouterListResourcesDesc, ListResourcesRequest.fromPartial(request), metadata);
  }
}

export const McpRouterDesc = { serviceName: "mcpany.mcp_router.v1.McpRouter" };

export const McpRouterListToolsDesc: UnaryMethodDefinitionish = {
  methodName: "ListTools",
  service: McpRouterDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return ListToolsRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = ListToolsResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const McpRouterCallToolDesc: UnaryMethodDefinitionish = {
  methodName: "CallTool",
  service: McpRouterDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return CallToolRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = CallToolResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const McpRouterListPromptsDesc: UnaryMethodDefinitionish = {
  methodName: "ListPrompts",
  service: McpRouterDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return ListPromptsRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = ListPromptsResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const McpRouterGetPromptDesc: UnaryMethodDefinitionish = {
  methodName: "GetPrompt",
  service: McpRouterDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return GetPromptRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = GetPromptResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const McpRouterListResourcesDesc: UnaryMethodDefinitionish = {
  methodName: "ListResources",
  service: McpRouterDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return ListResourcesRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = ListResourcesResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

interface UnaryMethodDefinitionishR extends grpc.UnaryMethodDefinition<any, any> {
  requestStream: any;
  responseStream: any;
}

type UnaryMethodDefinitionish = UnaryMethodDefinitionishR;

interface Rpc {
  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any>;
}

export class GrpcWebImpl {
  private host: string;
  private options: {
    transport?: grpc.TransportFactory;

    debug?: boolean;
    metadata?: grpc.Metadata;
    upStreamRetryCodes?: number[];
  };

  constructor(
    host: string,
    options: {
      transport?: grpc.TransportFactory;

      debug?: boolean;
      metadata?: grpc.Metadata;
      upStreamRetryCodes?: number[];
    },
  ) {
    this.host = host;
    this.options = options;
  }

  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    _request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any> {
    const request = { ..._request, ...methodDesc.requestType };
    const maybeCombinedMetadata = metadata && this.options.metadata
      ? new BrowserHeaders({ ...this.options?.metadata.headersMap, ...metadata?.headersMap })
      : metadata ?? this.options.metadata;
    return new Promise((resolve, reject) => {
      grpc.unary(methodDesc, {
        request,
        host: this.host,
        metadata: maybeCombinedMetadata ?? {},
        ...(this.options.transport !== undefined ? { transport: this.options.transport } : {}),
        debug: this.options.debug ?? false,
        onEnd: function (response) {
          if (response.status === grpc.Code.OK) {
            resolve(response.message!.toObject());
          } else {
            const err = new GrpcWebError(response.statusMessage, response.status, response.trailers);
            reject(err);
          }
        },
      });
    });
  }
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export class GrpcWebError extends globalThis.Error {
  constructor(message: string, public code: grpc.Code, public metadata: grpc.Metadata) {
    super(message);
  }
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
