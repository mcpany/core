// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.1
// source: proto/api/v1/skill_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { grpc } from "@improbable-eng/grpc-web";
import { BrowserHeaders } from "browser-headers";
import Long from "long";
import { Skill } from "../../config/v1/skill";

export const protobufPackage = "mcpany.api.v1";

export interface ListSkillsRequest {
}

export interface ListSkillsResponse {
  skills: Skill[];
}

export interface GetSkillRequest {
  name: string;
}

export interface GetSkillResponse {
  skill?: Skill | undefined;
}

export interface CreateSkillRequest {
  skill?: Skill | undefined;
}

export interface CreateSkillResponse {
  skill?: Skill | undefined;
}

export interface UpdateSkillRequest {
  name: string;
  skill?: Skill | undefined;
}

export interface UpdateSkillResponse {
  skill?: Skill | undefined;
}

export interface DeleteSkillRequest {
  name: string;
}

/** Empty */
export interface DeleteSkillResponse {
}

function createBaseListSkillsRequest(): ListSkillsRequest {
  return {};
}

export const ListSkillsRequest: MessageFns<ListSkillsRequest> = {
  encode(_: ListSkillsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSkillsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSkillsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListSkillsRequest {
    return {};
  },

  toJSON(_: ListSkillsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListSkillsRequest>, I>>(base?: I): ListSkillsRequest {
    return ListSkillsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListSkillsRequest>, I>>(_: I): ListSkillsRequest {
    const message = createBaseListSkillsRequest();
    return message;
  },
};

function createBaseListSkillsResponse(): ListSkillsResponse {
  return { skills: [] };
}

export const ListSkillsResponse: MessageFns<ListSkillsResponse> = {
  encode(message: ListSkillsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.skills) {
      Skill.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSkillsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSkillsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.skills.push(Skill.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSkillsResponse {
    return { skills: globalThis.Array.isArray(object?.skills) ? object.skills.map((e: any) => Skill.fromJSON(e)) : [] };
  },

  toJSON(message: ListSkillsResponse): unknown {
    const obj: any = {};
    if (message.skills?.length) {
      obj.skills = message.skills.map((e) => Skill.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListSkillsResponse>, I>>(base?: I): ListSkillsResponse {
    return ListSkillsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListSkillsResponse>, I>>(object: I): ListSkillsResponse {
    const message = createBaseListSkillsResponse();
    message.skills = object.skills?.map((e) => Skill.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetSkillRequest(): GetSkillRequest {
  return { name: "" };
}

export const GetSkillRequest: MessageFns<GetSkillRequest> = {
  encode(message: GetSkillRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSkillRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSkillRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSkillRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: GetSkillRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSkillRequest>, I>>(base?: I): GetSkillRequest {
    return GetSkillRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSkillRequest>, I>>(object: I): GetSkillRequest {
    const message = createBaseGetSkillRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseGetSkillResponse(): GetSkillResponse {
  return { skill: undefined };
}

export const GetSkillResponse: MessageFns<GetSkillResponse> = {
  encode(message: GetSkillResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.skill !== undefined) {
      Skill.encode(message.skill, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSkillResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSkillResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.skill = Skill.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSkillResponse {
    return { skill: isSet(object.skill) ? Skill.fromJSON(object.skill) : undefined };
  },

  toJSON(message: GetSkillResponse): unknown {
    const obj: any = {};
    if (message.skill !== undefined) {
      obj.skill = Skill.toJSON(message.skill);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSkillResponse>, I>>(base?: I): GetSkillResponse {
    return GetSkillResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSkillResponse>, I>>(object: I): GetSkillResponse {
    const message = createBaseGetSkillResponse();
    message.skill = (object.skill !== undefined && object.skill !== null) ? Skill.fromPartial(object.skill) : undefined;
    return message;
  },
};

function createBaseCreateSkillRequest(): CreateSkillRequest {
  return { skill: undefined };
}

export const CreateSkillRequest: MessageFns<CreateSkillRequest> = {
  encode(message: CreateSkillRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.skill !== undefined) {
      Skill.encode(message.skill, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSkillRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSkillRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.skill = Skill.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSkillRequest {
    return { skill: isSet(object.skill) ? Skill.fromJSON(object.skill) : undefined };
  },

  toJSON(message: CreateSkillRequest): unknown {
    const obj: any = {};
    if (message.skill !== undefined) {
      obj.skill = Skill.toJSON(message.skill);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateSkillRequest>, I>>(base?: I): CreateSkillRequest {
    return CreateSkillRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateSkillRequest>, I>>(object: I): CreateSkillRequest {
    const message = createBaseCreateSkillRequest();
    message.skill = (object.skill !== undefined && object.skill !== null) ? Skill.fromPartial(object.skill) : undefined;
    return message;
  },
};

function createBaseCreateSkillResponse(): CreateSkillResponse {
  return { skill: undefined };
}

export const CreateSkillResponse: MessageFns<CreateSkillResponse> = {
  encode(message: CreateSkillResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.skill !== undefined) {
      Skill.encode(message.skill, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSkillResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSkillResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.skill = Skill.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSkillResponse {
    return { skill: isSet(object.skill) ? Skill.fromJSON(object.skill) : undefined };
  },

  toJSON(message: CreateSkillResponse): unknown {
    const obj: any = {};
    if (message.skill !== undefined) {
      obj.skill = Skill.toJSON(message.skill);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateSkillResponse>, I>>(base?: I): CreateSkillResponse {
    return CreateSkillResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateSkillResponse>, I>>(object: I): CreateSkillResponse {
    const message = createBaseCreateSkillResponse();
    message.skill = (object.skill !== undefined && object.skill !== null) ? Skill.fromPartial(object.skill) : undefined;
    return message;
  },
};

function createBaseUpdateSkillRequest(): UpdateSkillRequest {
  return { name: "", skill: undefined };
}

export const UpdateSkillRequest: MessageFns<UpdateSkillRequest> = {
  encode(message: UpdateSkillRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.skill !== undefined) {
      Skill.encode(message.skill, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateSkillRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateSkillRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.skill = Skill.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateSkillRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      skill: isSet(object.skill) ? Skill.fromJSON(object.skill) : undefined,
    };
  },

  toJSON(message: UpdateSkillRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.skill !== undefined) {
      obj.skill = Skill.toJSON(message.skill);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateSkillRequest>, I>>(base?: I): UpdateSkillRequest {
    return UpdateSkillRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateSkillRequest>, I>>(object: I): UpdateSkillRequest {
    const message = createBaseUpdateSkillRequest();
    message.name = object.name ?? "";
    message.skill = (object.skill !== undefined && object.skill !== null) ? Skill.fromPartial(object.skill) : undefined;
    return message;
  },
};

function createBaseUpdateSkillResponse(): UpdateSkillResponse {
  return { skill: undefined };
}

export const UpdateSkillResponse: MessageFns<UpdateSkillResponse> = {
  encode(message: UpdateSkillResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.skill !== undefined) {
      Skill.encode(message.skill, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateSkillResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateSkillResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.skill = Skill.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateSkillResponse {
    return { skill: isSet(object.skill) ? Skill.fromJSON(object.skill) : undefined };
  },

  toJSON(message: UpdateSkillResponse): unknown {
    const obj: any = {};
    if (message.skill !== undefined) {
      obj.skill = Skill.toJSON(message.skill);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateSkillResponse>, I>>(base?: I): UpdateSkillResponse {
    return UpdateSkillResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateSkillResponse>, I>>(object: I): UpdateSkillResponse {
    const message = createBaseUpdateSkillResponse();
    message.skill = (object.skill !== undefined && object.skill !== null) ? Skill.fromPartial(object.skill) : undefined;
    return message;
  },
};

function createBaseDeleteSkillRequest(): DeleteSkillRequest {
  return { name: "" };
}

export const DeleteSkillRequest: MessageFns<DeleteSkillRequest> = {
  encode(message: DeleteSkillRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteSkillRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteSkillRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteSkillRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: DeleteSkillRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteSkillRequest>, I>>(base?: I): DeleteSkillRequest {
    return DeleteSkillRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteSkillRequest>, I>>(object: I): DeleteSkillRequest {
    const message = createBaseDeleteSkillRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseDeleteSkillResponse(): DeleteSkillResponse {
  return {};
}

export const DeleteSkillResponse: MessageFns<DeleteSkillResponse> = {
  encode(_: DeleteSkillResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteSkillResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteSkillResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): DeleteSkillResponse {
    return {};
  },

  toJSON(_: DeleteSkillResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteSkillResponse>, I>>(base?: I): DeleteSkillResponse {
    return DeleteSkillResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteSkillResponse>, I>>(_: I): DeleteSkillResponse {
    const message = createBaseDeleteSkillResponse();
    return message;
  },
};

export interface SkillService {
  ListSkills(request: DeepPartial<ListSkillsRequest>, metadata?: grpc.Metadata): Promise<ListSkillsResponse>;
  GetSkill(request: DeepPartial<GetSkillRequest>, metadata?: grpc.Metadata): Promise<GetSkillResponse>;
  CreateSkill(request: DeepPartial<CreateSkillRequest>, metadata?: grpc.Metadata): Promise<CreateSkillResponse>;
  UpdateSkill(request: DeepPartial<UpdateSkillRequest>, metadata?: grpc.Metadata): Promise<UpdateSkillResponse>;
  DeleteSkill(request: DeepPartial<DeleteSkillRequest>, metadata?: grpc.Metadata): Promise<DeleteSkillResponse>;
}

export class SkillServiceClientImpl implements SkillService {
  private readonly rpc: Rpc;

  constructor(rpc: Rpc) {
    this.rpc = rpc;
    this.ListSkills = this.ListSkills.bind(this);
    this.GetSkill = this.GetSkill.bind(this);
    this.CreateSkill = this.CreateSkill.bind(this);
    this.UpdateSkill = this.UpdateSkill.bind(this);
    this.DeleteSkill = this.DeleteSkill.bind(this);
  }

  ListSkills(request: DeepPartial<ListSkillsRequest>, metadata?: grpc.Metadata): Promise<ListSkillsResponse> {
    return this.rpc.unary(SkillServiceListSkillsDesc, ListSkillsRequest.fromPartial(request), metadata);
  }

  GetSkill(request: DeepPartial<GetSkillRequest>, metadata?: grpc.Metadata): Promise<GetSkillResponse> {
    return this.rpc.unary(SkillServiceGetSkillDesc, GetSkillRequest.fromPartial(request), metadata);
  }

  CreateSkill(request: DeepPartial<CreateSkillRequest>, metadata?: grpc.Metadata): Promise<CreateSkillResponse> {
    return this.rpc.unary(SkillServiceCreateSkillDesc, CreateSkillRequest.fromPartial(request), metadata);
  }

  UpdateSkill(request: DeepPartial<UpdateSkillRequest>, metadata?: grpc.Metadata): Promise<UpdateSkillResponse> {
    return this.rpc.unary(SkillServiceUpdateSkillDesc, UpdateSkillRequest.fromPartial(request), metadata);
  }

  DeleteSkill(request: DeepPartial<DeleteSkillRequest>, metadata?: grpc.Metadata): Promise<DeleteSkillResponse> {
    return this.rpc.unary(SkillServiceDeleteSkillDesc, DeleteSkillRequest.fromPartial(request), metadata);
  }
}

export const SkillServiceDesc = { serviceName: "mcpany.api.v1.SkillService" };

export const SkillServiceListSkillsDesc: UnaryMethodDefinitionish = {
  methodName: "ListSkills",
  service: SkillServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return ListSkillsRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = ListSkillsResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const SkillServiceGetSkillDesc: UnaryMethodDefinitionish = {
  methodName: "GetSkill",
  service: SkillServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return GetSkillRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = GetSkillResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const SkillServiceCreateSkillDesc: UnaryMethodDefinitionish = {
  methodName: "CreateSkill",
  service: SkillServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return CreateSkillRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = CreateSkillResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const SkillServiceUpdateSkillDesc: UnaryMethodDefinitionish = {
  methodName: "UpdateSkill",
  service: SkillServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return UpdateSkillRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = UpdateSkillResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const SkillServiceDeleteSkillDesc: UnaryMethodDefinitionish = {
  methodName: "DeleteSkill",
  service: SkillServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return DeleteSkillRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = DeleteSkillResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

interface UnaryMethodDefinitionishR extends grpc.UnaryMethodDefinition<any, any> {
  requestStream: any;
  responseStream: any;
}

type UnaryMethodDefinitionish = UnaryMethodDefinitionishR;

interface Rpc {
  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any>;
}

export class GrpcWebImpl {
  private host: string;
  private options: {
    transport?: grpc.TransportFactory;

    debug?: boolean;
    metadata?: grpc.Metadata;
    upStreamRetryCodes?: number[];
  };

  constructor(
    host: string,
    options: {
      transport?: grpc.TransportFactory;

      debug?: boolean;
      metadata?: grpc.Metadata;
      upStreamRetryCodes?: number[];
    },
  ) {
    this.host = host;
    this.options = options;
  }

  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    _request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any> {
    const request = { ..._request, ...methodDesc.requestType };
    const maybeCombinedMetadata = metadata && this.options.metadata
      ? new BrowserHeaders({ ...this.options?.metadata.headersMap, ...metadata?.headersMap })
      : metadata ?? this.options.metadata;
    return new Promise((resolve, reject) => {
      grpc.unary(methodDesc, {
        request,
        host: this.host,
        metadata: maybeCombinedMetadata ?? {},
        ...(this.options.transport !== undefined ? { transport: this.options.transport } : {}),
        debug: this.options.debug ?? false,
        onEnd: function (response) {
          if (response.status === grpc.Code.OK) {
            resolve(response.message!.toObject());
          } else {
            const err = new GrpcWebError(response.statusMessage, response.status, response.trailers);
            reject(err);
          }
        },
      });
    });
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export class GrpcWebError extends globalThis.Error {
  constructor(message: string, public code: grpc.Code, public metadata: grpc.Metadata) {
    super(message);
  }
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
