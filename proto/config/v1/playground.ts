// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.1
// source: proto/config/v1/playground.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Struct } from "../../google/protobuf/struct";

export const protobufPackage = "mcpany.config.v1";

/** SavedRequest represents a saved tool execution request. */
export interface SavedRequest {
  id: string;
  name: string;
  toolName: string;
  args?: { [key: string]: any } | undefined;
  createdAt: string;
}

/** RequestCollection represents a collection of saved requests. */
export interface RequestCollection {
  id: string;
  name: string;
  description: string;
  requests: SavedRequest[];
  createdAt: string;
  updatedAt: string;
}

function createBaseSavedRequest(): SavedRequest {
  return { id: "", name: "", toolName: "", args: undefined, createdAt: "" };
}

export const SavedRequest: MessageFns<SavedRequest> = {
  encode(message: SavedRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.toolName !== "") {
      writer.uint32(26).string(message.toolName);
    }
    if (message.args !== undefined) {
      Struct.encode(Struct.wrap(message.args), writer.uint32(34).fork()).join();
    }
    if (message.createdAt !== "") {
      writer.uint32(42).string(message.createdAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SavedRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSavedRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.toolName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.args = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SavedRequest {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      toolName: isSet(object.tool_name) ? globalThis.String(object.tool_name) : "",
      args: isObject(object.args) ? object.args : undefined,
      createdAt: isSet(object.created_at) ? globalThis.String(object.created_at) : "",
    };
  },

  toJSON(message: SavedRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.toolName !== "") {
      obj.tool_name = message.toolName;
    }
    if (message.args !== undefined) {
      obj.args = message.args;
    }
    if (message.createdAt !== "") {
      obj.created_at = message.createdAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SavedRequest>, I>>(base?: I): SavedRequest {
    return SavedRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SavedRequest>, I>>(object: I): SavedRequest {
    const message = createBaseSavedRequest();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.toolName = object.toolName ?? "";
    message.args = object.args ?? undefined;
    message.createdAt = object.createdAt ?? "";
    return message;
  },
};

function createBaseRequestCollection(): RequestCollection {
  return { id: "", name: "", description: "", requests: [], createdAt: "", updatedAt: "" };
}

export const RequestCollection: MessageFns<RequestCollection> = {
  encode(message: RequestCollection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    for (const v of message.requests) {
      SavedRequest.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.createdAt !== "") {
      writer.uint32(42).string(message.createdAt);
    }
    if (message.updatedAt !== "") {
      writer.uint32(50).string(message.updatedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestCollection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestCollection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.requests.push(SavedRequest.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestCollection {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      requests: globalThis.Array.isArray(object?.requests)
        ? object.requests.map((e: any) => SavedRequest.fromJSON(e))
        : [],
      createdAt: isSet(object.created_at) ? globalThis.String(object.created_at) : "",
      updatedAt: isSet(object.updated_at) ? globalThis.String(object.updated_at) : "",
    };
  },

  toJSON(message: RequestCollection): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.requests?.length) {
      obj.requests = message.requests.map((e) => SavedRequest.toJSON(e));
    }
    if (message.createdAt !== "") {
      obj.created_at = message.createdAt;
    }
    if (message.updatedAt !== "") {
      obj.updated_at = message.updatedAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestCollection>, I>>(base?: I): RequestCollection {
    return RequestCollection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestCollection>, I>>(object: I): RequestCollection {
    const message = createBaseRequestCollection();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.requests = object.requests?.map((e) => SavedRequest.fromPartial(e)) || [];
    message.createdAt = object.createdAt ?? "";
    message.updatedAt = object.updatedAt ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
