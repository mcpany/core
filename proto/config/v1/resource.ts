// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.1
// source: proto/config/v1/resource.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Timestamp } from "../../google/protobuf/timestamp";
import {
  CommandLineCallDefinition,
  GrpcCallDefinition,
  HttpCallDefinition,
  MCPCallDefinition,
  WebrtcCallDefinition,
  WebsocketCallDefinition,
} from "./call";
import { Profile } from "./profile";

export const protobufPackage = "mcpany.config.v1";

export interface ResourceAnnotation {
  /**
   * An array indicating the intended audience(s) for this
   * resource. Valid values are "user" and "assistant". For example,
   * ["user", "assistant"] indicates content useful for both.
   */
  audience: string[];
  /**
   * A number from 0.0 to 1.0 indicating the importance of
   * this resource. A value of 1 means “most important” (effectively
   * required), while 0 means “least important” (entirely optional).
   */
  priority: number;
  /**
   * An ISO 8601 formatted timestamp indicating when the
   * resource was last modified (e.g., "2025-01-12T15:00:58Z").
   */
  lastModified?: Date | undefined;
}

export interface ResourceDefinition {
  uri: string;
  name: string;
  title: string;
  description: string;
  mimeType: string;
  size: Long;
  annotations?: ResourceAnnotation | undefined;
  static?: StaticResource | undefined;
  dynamic?:
    | DynamicResource
    | undefined;
  /** If true, this resource is disabled. */
  disable: boolean;
  /** A list of profiles this resource belongs to. */
  profiles: Profile[];
}

export interface StaticResource {
  textContent?: string | undefined;
  binaryContent?: Uint8Array | undefined;
}

export interface DynamicResource {
  httpCall?: HttpCallDefinition | undefined;
  grpcCall?: GrpcCallDefinition | undefined;
  websocketCall?: WebsocketCallDefinition | undefined;
  webrtcCall?: WebrtcCallDefinition | undefined;
  commandLineCall?: CommandLineCallDefinition | undefined;
  mcpCall?: MCPCallDefinition | undefined;
}

function createBaseResourceAnnotation(): ResourceAnnotation {
  return { audience: [], priority: 0, lastModified: undefined };
}

export const ResourceAnnotation: MessageFns<ResourceAnnotation> = {
  encode(message: ResourceAnnotation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.audience) {
      writer.uint32(10).string(v!);
    }
    if (message.priority !== 0) {
      writer.uint32(17).double(message.priority);
    }
    if (message.lastModified !== undefined) {
      Timestamp.encode(toTimestamp(message.lastModified), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResourceAnnotation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResourceAnnotation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audience.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.priority = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.lastModified = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResourceAnnotation {
    return {
      audience: globalThis.Array.isArray(object?.audience) ? object.audience.map((e: any) => globalThis.String(e)) : [],
      priority: isSet(object.priority) ? globalThis.Number(object.priority) : 0,
      lastModified: isSet(object.lastModified)
        ? fromJsonTimestamp(object.lastModified)
        : isSet(object.last_modified)
        ? fromJsonTimestamp(object.last_modified)
        : undefined,
    };
  },

  toJSON(message: ResourceAnnotation): unknown {
    const obj: any = {};
    if (message.audience?.length) {
      obj.audience = message.audience;
    }
    if (message.priority !== 0) {
      obj.priority = message.priority;
    }
    if (message.lastModified !== undefined) {
      obj.lastModified = message.lastModified.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResourceAnnotation>, I>>(base?: I): ResourceAnnotation {
    return ResourceAnnotation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResourceAnnotation>, I>>(object: I): ResourceAnnotation {
    const message = createBaseResourceAnnotation();
    message.audience = object.audience?.map((e) => e) || [];
    message.priority = object.priority ?? 0;
    message.lastModified = object.lastModified ?? undefined;
    return message;
  },
};

function createBaseResourceDefinition(): ResourceDefinition {
  return {
    uri: "",
    name: "",
    title: "",
    description: "",
    mimeType: "",
    size: Long.ZERO,
    annotations: undefined,
    static: undefined,
    dynamic: undefined,
    disable: false,
    profiles: [],
  };
}

export const ResourceDefinition: MessageFns<ResourceDefinition> = {
  encode(message: ResourceDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uri !== "") {
      writer.uint32(10).string(message.uri);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.title !== "") {
      writer.uint32(26).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    if (message.mimeType !== "") {
      writer.uint32(42).string(message.mimeType);
    }
    if (!message.size.equals(Long.ZERO)) {
      writer.uint32(48).int64(message.size.toString());
    }
    if (message.annotations !== undefined) {
      ResourceAnnotation.encode(message.annotations, writer.uint32(58).fork()).join();
    }
    if (message.static !== undefined) {
      StaticResource.encode(message.static, writer.uint32(66).fork()).join();
    }
    if (message.dynamic !== undefined) {
      DynamicResource.encode(message.dynamic, writer.uint32(74).fork()).join();
    }
    if (message.disable !== false) {
      writer.uint32(80).bool(message.disable);
    }
    for (const v of message.profiles) {
      Profile.encode(v!, writer.uint32(90).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResourceDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResourceDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uri = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.size = Long.fromString(reader.int64().toString());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.annotations = ResourceAnnotation.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.static = StaticResource.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.dynamic = DynamicResource.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.disable = reader.bool();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.profiles.push(Profile.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResourceDefinition {
    return {
      uri: isSet(object.uri) ? globalThis.String(object.uri) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      mimeType: isSet(object.mimeType)
        ? globalThis.String(object.mimeType)
        : isSet(object.mime_type)
        ? globalThis.String(object.mime_type)
        : "",
      size: isSet(object.size) ? Long.fromValue(object.size) : Long.ZERO,
      annotations: isSet(object.annotations) ? ResourceAnnotation.fromJSON(object.annotations) : undefined,
      static: isSet(object.static) ? StaticResource.fromJSON(object.static) : undefined,
      dynamic: isSet(object.dynamic) ? DynamicResource.fromJSON(object.dynamic) : undefined,
      disable: isSet(object.disable) ? globalThis.Boolean(object.disable) : false,
      profiles: globalThis.Array.isArray(object?.profiles) ? object.profiles.map((e: any) => Profile.fromJSON(e)) : [],
    };
  },

  toJSON(message: ResourceDefinition): unknown {
    const obj: any = {};
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    if (!message.size.equals(Long.ZERO)) {
      obj.size = (message.size || Long.ZERO).toString();
    }
    if (message.annotations !== undefined) {
      obj.annotations = ResourceAnnotation.toJSON(message.annotations);
    }
    if (message.static !== undefined) {
      obj.static = StaticResource.toJSON(message.static);
    }
    if (message.dynamic !== undefined) {
      obj.dynamic = DynamicResource.toJSON(message.dynamic);
    }
    if (message.disable !== false) {
      obj.disable = message.disable;
    }
    if (message.profiles?.length) {
      obj.profiles = message.profiles.map((e) => Profile.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResourceDefinition>, I>>(base?: I): ResourceDefinition {
    return ResourceDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResourceDefinition>, I>>(object: I): ResourceDefinition {
    const message = createBaseResourceDefinition();
    message.uri = object.uri ?? "";
    message.name = object.name ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.mimeType = object.mimeType ?? "";
    message.size = (object.size !== undefined && object.size !== null) ? Long.fromValue(object.size) : Long.ZERO;
    message.annotations = (object.annotations !== undefined && object.annotations !== null)
      ? ResourceAnnotation.fromPartial(object.annotations)
      : undefined;
    message.static = (object.static !== undefined && object.static !== null)
      ? StaticResource.fromPartial(object.static)
      : undefined;
    message.dynamic = (object.dynamic !== undefined && object.dynamic !== null)
      ? DynamicResource.fromPartial(object.dynamic)
      : undefined;
    message.disable = object.disable ?? false;
    message.profiles = object.profiles?.map((e) => Profile.fromPartial(e)) || [];
    return message;
  },
};

function createBaseStaticResource(): StaticResource {
  return { textContent: undefined, binaryContent: undefined };
}

export const StaticResource: MessageFns<StaticResource> = {
  encode(message: StaticResource, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.textContent !== undefined) {
      writer.uint32(10).string(message.textContent);
    }
    if (message.binaryContent !== undefined) {
      writer.uint32(18).bytes(message.binaryContent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StaticResource {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStaticResource();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.textContent = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.binaryContent = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StaticResource {
    return {
      textContent: isSet(object.textContent)
        ? globalThis.String(object.textContent)
        : isSet(object.text_content)
        ? globalThis.String(object.text_content)
        : undefined,
      binaryContent: isSet(object.binaryContent)
        ? bytesFromBase64(object.binaryContent)
        : isSet(object.binary_content)
        ? bytesFromBase64(object.binary_content)
        : undefined,
    };
  },

  toJSON(message: StaticResource): unknown {
    const obj: any = {};
    if (message.textContent !== undefined) {
      obj.textContent = message.textContent;
    }
    if (message.binaryContent !== undefined) {
      obj.binaryContent = base64FromBytes(message.binaryContent);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StaticResource>, I>>(base?: I): StaticResource {
    return StaticResource.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StaticResource>, I>>(object: I): StaticResource {
    const message = createBaseStaticResource();
    message.textContent = object.textContent ?? undefined;
    message.binaryContent = object.binaryContent ?? undefined;
    return message;
  },
};

function createBaseDynamicResource(): DynamicResource {
  return {
    httpCall: undefined,
    grpcCall: undefined,
    websocketCall: undefined,
    webrtcCall: undefined,
    commandLineCall: undefined,
    mcpCall: undefined,
  };
}

export const DynamicResource: MessageFns<DynamicResource> = {
  encode(message: DynamicResource, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.httpCall !== undefined) {
      HttpCallDefinition.encode(message.httpCall, writer.uint32(10).fork()).join();
    }
    if (message.grpcCall !== undefined) {
      GrpcCallDefinition.encode(message.grpcCall, writer.uint32(18).fork()).join();
    }
    if (message.websocketCall !== undefined) {
      WebsocketCallDefinition.encode(message.websocketCall, writer.uint32(26).fork()).join();
    }
    if (message.webrtcCall !== undefined) {
      WebrtcCallDefinition.encode(message.webrtcCall, writer.uint32(34).fork()).join();
    }
    if (message.commandLineCall !== undefined) {
      CommandLineCallDefinition.encode(message.commandLineCall, writer.uint32(42).fork()).join();
    }
    if (message.mcpCall !== undefined) {
      MCPCallDefinition.encode(message.mcpCall, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DynamicResource {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDynamicResource();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.httpCall = HttpCallDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.grpcCall = GrpcCallDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.websocketCall = WebsocketCallDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.webrtcCall = WebrtcCallDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.commandLineCall = CommandLineCallDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.mcpCall = MCPCallDefinition.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DynamicResource {
    return {
      httpCall: isSet(object.httpCall)
        ? HttpCallDefinition.fromJSON(object.httpCall)
        : isSet(object.http_call)
        ? HttpCallDefinition.fromJSON(object.http_call)
        : undefined,
      grpcCall: isSet(object.grpcCall)
        ? GrpcCallDefinition.fromJSON(object.grpcCall)
        : isSet(object.grpc_call)
        ? GrpcCallDefinition.fromJSON(object.grpc_call)
        : undefined,
      websocketCall: isSet(object.websocketCall)
        ? WebsocketCallDefinition.fromJSON(object.websocketCall)
        : isSet(object.websocket_call)
        ? WebsocketCallDefinition.fromJSON(object.websocket_call)
        : undefined,
      webrtcCall: isSet(object.webrtcCall)
        ? WebrtcCallDefinition.fromJSON(object.webrtcCall)
        : isSet(object.webrtc_call)
        ? WebrtcCallDefinition.fromJSON(object.webrtc_call)
        : undefined,
      commandLineCall: isSet(object.commandLineCall)
        ? CommandLineCallDefinition.fromJSON(object.commandLineCall)
        : isSet(object.command_line_call)
        ? CommandLineCallDefinition.fromJSON(object.command_line_call)
        : undefined,
      mcpCall: isSet(object.mcpCall)
        ? MCPCallDefinition.fromJSON(object.mcpCall)
        : isSet(object.mcp_call)
        ? MCPCallDefinition.fromJSON(object.mcp_call)
        : undefined,
    };
  },

  toJSON(message: DynamicResource): unknown {
    const obj: any = {};
    if (message.httpCall !== undefined) {
      obj.httpCall = HttpCallDefinition.toJSON(message.httpCall);
    }
    if (message.grpcCall !== undefined) {
      obj.grpcCall = GrpcCallDefinition.toJSON(message.grpcCall);
    }
    if (message.websocketCall !== undefined) {
      obj.websocketCall = WebsocketCallDefinition.toJSON(message.websocketCall);
    }
    if (message.webrtcCall !== undefined) {
      obj.webrtcCall = WebrtcCallDefinition.toJSON(message.webrtcCall);
    }
    if (message.commandLineCall !== undefined) {
      obj.commandLineCall = CommandLineCallDefinition.toJSON(message.commandLineCall);
    }
    if (message.mcpCall !== undefined) {
      obj.mcpCall = MCPCallDefinition.toJSON(message.mcpCall);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DynamicResource>, I>>(base?: I): DynamicResource {
    return DynamicResource.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DynamicResource>, I>>(object: I): DynamicResource {
    const message = createBaseDynamicResource();
    message.httpCall = (object.httpCall !== undefined && object.httpCall !== null)
      ? HttpCallDefinition.fromPartial(object.httpCall)
      : undefined;
    message.grpcCall = (object.grpcCall !== undefined && object.grpcCall !== null)
      ? GrpcCallDefinition.fromPartial(object.grpcCall)
      : undefined;
    message.websocketCall = (object.websocketCall !== undefined && object.websocketCall !== null)
      ? WebsocketCallDefinition.fromPartial(object.websocketCall)
      : undefined;
    message.webrtcCall = (object.webrtcCall !== undefined && object.webrtcCall !== null)
      ? WebrtcCallDefinition.fromPartial(object.webrtcCall)
      : undefined;
    message.commandLineCall = (object.commandLineCall !== undefined && object.commandLineCall !== null)
      ? CommandLineCallDefinition.fromPartial(object.commandLineCall)
      : undefined;
    message.mcpCall = (object.mcpCall !== undefined && object.mcpCall !== null)
      ? MCPCallDefinition.fromPartial(object.mcpCall)
      : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = numberToLong(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds.toNumber() || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function numberToLong(number: number) {
  return Long.fromNumber(number);
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
