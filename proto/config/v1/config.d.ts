// Copyright 2026 Author(s) of MCP Any
// SPDX-License-Identifier: Apache-2.0
// TypeScript declaration stubs for proto/config/v1/config.proto
// These declarations provide type information for the Bazel build environment.
// The full implementation is generated by ts-proto via `make gen`.

import type { SecretValue } from './auth';

export interface ProfileSelector {
  tags?: string[];
  toolProperties?: Record<string, string>;
}

export interface ProfileServiceConfig {
  [key: string]: unknown;
}

export interface ProfileDefinition {
  name?: string;
  selector?: ProfileSelector;
  requiredRoles?: string[];
  parentProfileIds?: string[];
  serviceConfig?: Record<string, ProfileServiceConfig>;
  secrets?: Record<string, SecretValue>;
}
