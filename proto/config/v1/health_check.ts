// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.1
// source: proto/config/v1/health_check.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Duration } from "../../google/protobuf/duration";

export const protobufPackage = "mcpany.config.v1";

/** HttpHealthCheck defines a health check mechanism using HTTP requests. */
export interface HttpHealthCheck {
  /** The full URL to send the health check request to. */
  url: string;
  /** The expected HTTP status code for a successful check (default: 200). */
  expectedCode: number;
  /** A substring that must be present in the response body for the check to pass. */
  expectedResponseBodyContains: string;
  /** The frequency at which to perform the health check. */
  interval?:
    | Duration
    | undefined;
  /** The maximum time to wait for a response before considering the check failed. */
  timeout?:
    | Duration
    | undefined;
  /** The HTTP method to use (e.g., "GET", "HEAD"). Default is "GET". */
  method: string;
}

/** WebsocketHealthCheck defines a health check mechanism for Websocket services. */
export interface WebsocketHealthCheck {
  /** The message to send to the websocket server to initiate the check (e.g., "ping"). */
  message: string;
  /** A substring that must be present in the response message for the check to pass. */
  expectedResponseContains: string;
  /** The frequency at which to perform the health check. */
  interval?:
    | Duration
    | undefined;
  /** The maximum time to wait for a response before considering the check failed. */
  timeout?:
    | Duration
    | undefined;
  /** The full URL of the websocket endpoint. */
  url: string;
}

/** GrpcHealthCheck defines a health check mechanism for gRPC services, typically using the standard Health service. */
export interface GrpcHealthCheck {
  /** The name of the gRPC service to check (e.g., "grpc.health.v1.Health"). */
  service: string;
  /** The specific gRPC method to call. */
  method: string;
  /** The JSON representation of the request message. */
  request: string;
  /** The JSON representation of the expected response message. */
  expectedResponse: string;
  /** If true, the connection will use plaintext instead of TLS. */
  insecure: boolean;
  /** The frequency at which to perform the health check. */
  interval?:
    | Duration
    | undefined;
  /** The maximum time to wait for a response before considering the check failed. */
  timeout?: Duration | undefined;
}

/** CommandLineHealthCheck defines a health check mechanism for CLI-based services. */
export interface CommandLineHealthCheck {
  /** The command or method to invoke to check health. */
  method: string;
  /** The input or prompt to pass to the command. */
  prompt: string;
  /** A substring that must be present in the command's output for the check to pass. */
  expectedResponseContains: string;
  /** The frequency at which to perform the health check. */
  interval?:
    | Duration
    | undefined;
  /** The maximum time to wait for execution before considering the check failed. */
  timeout?: Duration | undefined;
}

/**
 * WebRTCHealthCheck defines a health check mechanism for WebRTC services.
 * It can use either HTTP or Websocket for the signaling check.
 */
export interface WebRTCHealthCheck {
  http?: HttpHealthCheck | undefined;
  websocket?: WebsocketHealthCheck | undefined;
}

function createBaseHttpHealthCheck(): HttpHealthCheck {
  return {
    url: "",
    expectedCode: 0,
    expectedResponseBodyContains: "",
    interval: undefined,
    timeout: undefined,
    method: "",
  };
}

export const HttpHealthCheck: MessageFns<HttpHealthCheck> = {
  encode(message: HttpHealthCheck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    if (message.expectedCode !== 0) {
      writer.uint32(16).int32(message.expectedCode);
    }
    if (message.expectedResponseBodyContains !== "") {
      writer.uint32(26).string(message.expectedResponseBodyContains);
    }
    if (message.interval !== undefined) {
      Duration.encode(message.interval, writer.uint32(34).fork()).join();
    }
    if (message.timeout !== undefined) {
      Duration.encode(message.timeout, writer.uint32(42).fork()).join();
    }
    if (message.method !== "") {
      writer.uint32(50).string(message.method);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpHealthCheck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpHealthCheck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expectedCode = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.expectedResponseBodyContains = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.interval = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.method = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HttpHealthCheck {
    return {
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      expectedCode: isSet(object.expectedCode)
        ? globalThis.Number(object.expectedCode)
        : isSet(object.expected_code)
        ? globalThis.Number(object.expected_code)
        : 0,
      expectedResponseBodyContains: isSet(object.expectedResponseBodyContains)
        ? globalThis.String(object.expectedResponseBodyContains)
        : isSet(object.expected_response_body_contains)
        ? globalThis.String(object.expected_response_body_contains)
        : "",
      interval: isSet(object.interval) ? Duration.fromJSON(object.interval) : undefined,
      timeout: isSet(object.timeout) ? Duration.fromJSON(object.timeout) : undefined,
      method: isSet(object.method) ? globalThis.String(object.method) : "",
    };
  },

  toJSON(message: HttpHealthCheck): unknown {
    const obj: any = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.expectedCode !== 0) {
      obj.expectedCode = Math.round(message.expectedCode);
    }
    if (message.expectedResponseBodyContains !== "") {
      obj.expectedResponseBodyContains = message.expectedResponseBodyContains;
    }
    if (message.interval !== undefined) {
      obj.interval = Duration.toJSON(message.interval);
    }
    if (message.timeout !== undefined) {
      obj.timeout = Duration.toJSON(message.timeout);
    }
    if (message.method !== "") {
      obj.method = message.method;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HttpHealthCheck>, I>>(base?: I): HttpHealthCheck {
    return HttpHealthCheck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HttpHealthCheck>, I>>(object: I): HttpHealthCheck {
    const message = createBaseHttpHealthCheck();
    message.url = object.url ?? "";
    message.expectedCode = object.expectedCode ?? 0;
    message.expectedResponseBodyContains = object.expectedResponseBodyContains ?? "";
    message.interval = (object.interval !== undefined && object.interval !== null)
      ? Duration.fromPartial(object.interval)
      : undefined;
    message.timeout = (object.timeout !== undefined && object.timeout !== null)
      ? Duration.fromPartial(object.timeout)
      : undefined;
    message.method = object.method ?? "";
    return message;
  },
};

function createBaseWebsocketHealthCheck(): WebsocketHealthCheck {
  return { message: "", expectedResponseContains: "", interval: undefined, timeout: undefined, url: "" };
}

export const WebsocketHealthCheck: MessageFns<WebsocketHealthCheck> = {
  encode(message: WebsocketHealthCheck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    if (message.expectedResponseContains !== "") {
      writer.uint32(18).string(message.expectedResponseContains);
    }
    if (message.interval !== undefined) {
      Duration.encode(message.interval, writer.uint32(26).fork()).join();
    }
    if (message.timeout !== undefined) {
      Duration.encode(message.timeout, writer.uint32(34).fork()).join();
    }
    if (message.url !== "") {
      writer.uint32(42).string(message.url);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WebsocketHealthCheck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWebsocketHealthCheck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.expectedResponseContains = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.interval = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WebsocketHealthCheck {
    return {
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      expectedResponseContains: isSet(object.expectedResponseContains)
        ? globalThis.String(object.expectedResponseContains)
        : isSet(object.expected_response_contains)
        ? globalThis.String(object.expected_response_contains)
        : "",
      interval: isSet(object.interval) ? Duration.fromJSON(object.interval) : undefined,
      timeout: isSet(object.timeout) ? Duration.fromJSON(object.timeout) : undefined,
      url: isSet(object.url) ? globalThis.String(object.url) : "",
    };
  },

  toJSON(message: WebsocketHealthCheck): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.expectedResponseContains !== "") {
      obj.expectedResponseContains = message.expectedResponseContains;
    }
    if (message.interval !== undefined) {
      obj.interval = Duration.toJSON(message.interval);
    }
    if (message.timeout !== undefined) {
      obj.timeout = Duration.toJSON(message.timeout);
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WebsocketHealthCheck>, I>>(base?: I): WebsocketHealthCheck {
    return WebsocketHealthCheck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WebsocketHealthCheck>, I>>(object: I): WebsocketHealthCheck {
    const message = createBaseWebsocketHealthCheck();
    message.message = object.message ?? "";
    message.expectedResponseContains = object.expectedResponseContains ?? "";
    message.interval = (object.interval !== undefined && object.interval !== null)
      ? Duration.fromPartial(object.interval)
      : undefined;
    message.timeout = (object.timeout !== undefined && object.timeout !== null)
      ? Duration.fromPartial(object.timeout)
      : undefined;
    message.url = object.url ?? "";
    return message;
  },
};

function createBaseGrpcHealthCheck(): GrpcHealthCheck {
  return {
    service: "",
    method: "",
    request: "",
    expectedResponse: "",
    insecure: false,
    interval: undefined,
    timeout: undefined,
  };
}

export const GrpcHealthCheck: MessageFns<GrpcHealthCheck> = {
  encode(message: GrpcHealthCheck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service !== "") {
      writer.uint32(10).string(message.service);
    }
    if (message.method !== "") {
      writer.uint32(18).string(message.method);
    }
    if (message.request !== "") {
      writer.uint32(26).string(message.request);
    }
    if (message.expectedResponse !== "") {
      writer.uint32(34).string(message.expectedResponse);
    }
    if (message.insecure !== false) {
      writer.uint32(40).bool(message.insecure);
    }
    if (message.interval !== undefined) {
      Duration.encode(message.interval, writer.uint32(50).fork()).join();
    }
    if (message.timeout !== undefined) {
      Duration.encode(message.timeout, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcHealthCheck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcHealthCheck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.service = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.request = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.expectedResponse = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.insecure = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.interval = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcHealthCheck {
    return {
      service: isSet(object.service) ? globalThis.String(object.service) : "",
      method: isSet(object.method) ? globalThis.String(object.method) : "",
      request: isSet(object.request) ? globalThis.String(object.request) : "",
      expectedResponse: isSet(object.expectedResponse)
        ? globalThis.String(object.expectedResponse)
        : isSet(object.expected_response)
        ? globalThis.String(object.expected_response)
        : "",
      insecure: isSet(object.insecure) ? globalThis.Boolean(object.insecure) : false,
      interval: isSet(object.interval) ? Duration.fromJSON(object.interval) : undefined,
      timeout: isSet(object.timeout) ? Duration.fromJSON(object.timeout) : undefined,
    };
  },

  toJSON(message: GrpcHealthCheck): unknown {
    const obj: any = {};
    if (message.service !== "") {
      obj.service = message.service;
    }
    if (message.method !== "") {
      obj.method = message.method;
    }
    if (message.request !== "") {
      obj.request = message.request;
    }
    if (message.expectedResponse !== "") {
      obj.expectedResponse = message.expectedResponse;
    }
    if (message.insecure !== false) {
      obj.insecure = message.insecure;
    }
    if (message.interval !== undefined) {
      obj.interval = Duration.toJSON(message.interval);
    }
    if (message.timeout !== undefined) {
      obj.timeout = Duration.toJSON(message.timeout);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcHealthCheck>, I>>(base?: I): GrpcHealthCheck {
    return GrpcHealthCheck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcHealthCheck>, I>>(object: I): GrpcHealthCheck {
    const message = createBaseGrpcHealthCheck();
    message.service = object.service ?? "";
    message.method = object.method ?? "";
    message.request = object.request ?? "";
    message.expectedResponse = object.expectedResponse ?? "";
    message.insecure = object.insecure ?? false;
    message.interval = (object.interval !== undefined && object.interval !== null)
      ? Duration.fromPartial(object.interval)
      : undefined;
    message.timeout = (object.timeout !== undefined && object.timeout !== null)
      ? Duration.fromPartial(object.timeout)
      : undefined;
    return message;
  },
};

function createBaseCommandLineHealthCheck(): CommandLineHealthCheck {
  return { method: "", prompt: "", expectedResponseContains: "", interval: undefined, timeout: undefined };
}

export const CommandLineHealthCheck: MessageFns<CommandLineHealthCheck> = {
  encode(message: CommandLineHealthCheck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.method !== "") {
      writer.uint32(10).string(message.method);
    }
    if (message.prompt !== "") {
      writer.uint32(18).string(message.prompt);
    }
    if (message.expectedResponseContains !== "") {
      writer.uint32(26).string(message.expectedResponseContains);
    }
    if (message.interval !== undefined) {
      Duration.encode(message.interval, writer.uint32(34).fork()).join();
    }
    if (message.timeout !== undefined) {
      Duration.encode(message.timeout, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandLineHealthCheck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandLineHealthCheck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.expectedResponseContains = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.interval = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommandLineHealthCheck {
    return {
      method: isSet(object.method) ? globalThis.String(object.method) : "",
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
      expectedResponseContains: isSet(object.expectedResponseContains)
        ? globalThis.String(object.expectedResponseContains)
        : isSet(object.expected_response_contains)
        ? globalThis.String(object.expected_response_contains)
        : "",
      interval: isSet(object.interval) ? Duration.fromJSON(object.interval) : undefined,
      timeout: isSet(object.timeout) ? Duration.fromJSON(object.timeout) : undefined,
    };
  },

  toJSON(message: CommandLineHealthCheck): unknown {
    const obj: any = {};
    if (message.method !== "") {
      obj.method = message.method;
    }
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    if (message.expectedResponseContains !== "") {
      obj.expectedResponseContains = message.expectedResponseContains;
    }
    if (message.interval !== undefined) {
      obj.interval = Duration.toJSON(message.interval);
    }
    if (message.timeout !== undefined) {
      obj.timeout = Duration.toJSON(message.timeout);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommandLineHealthCheck>, I>>(base?: I): CommandLineHealthCheck {
    return CommandLineHealthCheck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommandLineHealthCheck>, I>>(object: I): CommandLineHealthCheck {
    const message = createBaseCommandLineHealthCheck();
    message.method = object.method ?? "";
    message.prompt = object.prompt ?? "";
    message.expectedResponseContains = object.expectedResponseContains ?? "";
    message.interval = (object.interval !== undefined && object.interval !== null)
      ? Duration.fromPartial(object.interval)
      : undefined;
    message.timeout = (object.timeout !== undefined && object.timeout !== null)
      ? Duration.fromPartial(object.timeout)
      : undefined;
    return message;
  },
};

function createBaseWebRTCHealthCheck(): WebRTCHealthCheck {
  return { http: undefined, websocket: undefined };
}

export const WebRTCHealthCheck: MessageFns<WebRTCHealthCheck> = {
  encode(message: WebRTCHealthCheck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.http !== undefined) {
      HttpHealthCheck.encode(message.http, writer.uint32(10).fork()).join();
    }
    if (message.websocket !== undefined) {
      WebsocketHealthCheck.encode(message.websocket, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WebRTCHealthCheck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWebRTCHealthCheck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.http = HttpHealthCheck.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.websocket = WebsocketHealthCheck.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WebRTCHealthCheck {
    return {
      http: isSet(object.http) ? HttpHealthCheck.fromJSON(object.http) : undefined,
      websocket: isSet(object.websocket) ? WebsocketHealthCheck.fromJSON(object.websocket) : undefined,
    };
  },

  toJSON(message: WebRTCHealthCheck): unknown {
    const obj: any = {};
    if (message.http !== undefined) {
      obj.http = HttpHealthCheck.toJSON(message.http);
    }
    if (message.websocket !== undefined) {
      obj.websocket = WebsocketHealthCheck.toJSON(message.websocket);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WebRTCHealthCheck>, I>>(base?: I): WebRTCHealthCheck {
    return WebRTCHealthCheck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WebRTCHealthCheck>, I>>(object: I): WebRTCHealthCheck {
    const message = createBaseWebRTCHealthCheck();
    message.http = (object.http !== undefined && object.http !== null)
      ? HttpHealthCheck.fromPartial(object.http)
      : undefined;
    message.websocket = (object.websocket !== undefined && object.websocket !== null)
      ? WebsocketHealthCheck.fromPartial(object.websocket)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
