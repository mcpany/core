// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.1
// source: proto/config/v1/tool.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Struct } from "../../google/protobuf/struct";
import { Profile } from "./profile";

export const protobufPackage = "mcpany.config.v1";

/** ToolDefinition describes a single capability or "tool" offered by a service. */
export interface ToolDefinition {
  /** The name of the tool, which will be used to invoke it. */
  name: string;
  /** The input schema for the tool. */
  inputSchema?:
    | { [key: string]: any }
    | undefined;
  /** The ID of the service that provides the tool. */
  serviceId: string;
  /** A human-readable description of what the tool does. */
  description: string;
  /** Indicates if the tool produces a continuous stream of responses. */
  isStream: boolean;
  /** A human-readable title for the tool. */
  title: string;
  /**
   * If true, the tool does not modify its environment.
   * Default: false
   */
  readOnlyHint: boolean;
  /**
   * If true, the tool may perform destructive updates to its environment. If
   * false, the tool performs only additive updates.
   * (This property is meaningful only when ReadOnlyHint == false.)
   * Default: true
   */
  destructiveHint: boolean;
  /**
   * If true, calling the tool repeatedly with the same arguments will have no
   * additional effect on its environment.
   * (This property is meaningful only when ReadOnlyHint == false.)
   * Default: false
   */
  idempotentHint: boolean;
  /**
   * If true, this tool may interact with an "open world" of external entities. If
   * false, the tool's domain of interaction is closed. For example, the world of
   * a web search tool is open, whereas that of a memory tool is not.
   * Default: true
   */
  openWorldHint: boolean;
  /** The ID of the call definition to use for this tool. */
  callId: string;
  /** If true, this tool is disabled. */
  disable: boolean;
  /** A list of profiles this tool belongs to. */
  profiles: Profile[];
  mergeStrategy: ToolDefinition_MergeStrategy;
  tags: string[];
  /** Integrity check for the tool definition. */
  integrity?: Integrity | undefined;
}

export enum ToolDefinition_MergeStrategy {
  MERGE_STRATEGY_UNSPECIFIED = 0,
  MERGE_STRATEGY_MERGE = 1,
  MERGE_STRATEGY_OVERRIDE = 2,
  UNRECOGNIZED = -1,
}

export function toolDefinition_MergeStrategyFromJSON(object: any): ToolDefinition_MergeStrategy {
  switch (object) {
    case 0:
    case "MERGE_STRATEGY_UNSPECIFIED":
      return ToolDefinition_MergeStrategy.MERGE_STRATEGY_UNSPECIFIED;
    case 1:
    case "MERGE_STRATEGY_MERGE":
      return ToolDefinition_MergeStrategy.MERGE_STRATEGY_MERGE;
    case 2:
    case "MERGE_STRATEGY_OVERRIDE":
      return ToolDefinition_MergeStrategy.MERGE_STRATEGY_OVERRIDE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ToolDefinition_MergeStrategy.UNRECOGNIZED;
  }
}

export function toolDefinition_MergeStrategyToJSON(object: ToolDefinition_MergeStrategy): string {
  switch (object) {
    case ToolDefinition_MergeStrategy.MERGE_STRATEGY_UNSPECIFIED:
      return "MERGE_STRATEGY_UNSPECIFIED";
    case ToolDefinition_MergeStrategy.MERGE_STRATEGY_MERGE:
      return "MERGE_STRATEGY_MERGE";
    case ToolDefinition_MergeStrategy.MERGE_STRATEGY_OVERRIDE:
      return "MERGE_STRATEGY_OVERRIDE";
    case ToolDefinition_MergeStrategy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Integrity {
  hash: string;
  /** e.g. "sha256" */
  algorithm: string;
}

function createBaseToolDefinition(): ToolDefinition {
  return {
    name: "",
    inputSchema: undefined,
    serviceId: "",
    description: "",
    isStream: false,
    title: "",
    readOnlyHint: false,
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: false,
    callId: "",
    disable: false,
    profiles: [],
    mergeStrategy: 0,
    tags: [],
    integrity: undefined,
  };
}

export const ToolDefinition: MessageFns<ToolDefinition> = {
  encode(message: ToolDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.inputSchema !== undefined) {
      Struct.encode(Struct.wrap(message.inputSchema), writer.uint32(26).fork()).join();
    }
    if (message.serviceId !== "") {
      writer.uint32(106).string(message.serviceId);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.isStream !== false) {
      writer.uint32(40).bool(message.isStream);
    }
    if (message.title !== "") {
      writer.uint32(50).string(message.title);
    }
    if (message.readOnlyHint !== false) {
      writer.uint32(56).bool(message.readOnlyHint);
    }
    if (message.destructiveHint !== false) {
      writer.uint32(64).bool(message.destructiveHint);
    }
    if (message.idempotentHint !== false) {
      writer.uint32(72).bool(message.idempotentHint);
    }
    if (message.openWorldHint !== false) {
      writer.uint32(80).bool(message.openWorldHint);
    }
    if (message.callId !== "") {
      writer.uint32(90).string(message.callId);
    }
    if (message.disable !== false) {
      writer.uint32(96).bool(message.disable);
    }
    for (const v of message.profiles) {
      Profile.encode(v!, writer.uint32(114).fork()).join();
    }
    if (message.mergeStrategy !== 0) {
      writer.uint32(120).int32(message.mergeStrategy);
    }
    for (const v of message.tags) {
      writer.uint32(130).string(v!);
    }
    if (message.integrity !== undefined) {
      Integrity.encode(message.integrity, writer.uint32(138).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.inputSchema = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.serviceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isStream = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.readOnlyHint = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.destructiveHint = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.idempotentHint = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.openWorldHint = reader.bool();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.callId = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.disable = reader.bool();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.profiles.push(Profile.decode(reader, reader.uint32()));
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.mergeStrategy = reader.int32() as any;
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.tags.push(reader.string());
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.integrity = Integrity.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolDefinition {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      inputSchema: isObject(object.input_schema) ? object.input_schema : undefined,
      serviceId: isSet(object.service_id) ? globalThis.String(object.service_id) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      isStream: isSet(object.is_stream) ? globalThis.Boolean(object.is_stream) : false,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      readOnlyHint: isSet(object.read_only_hint) ? globalThis.Boolean(object.read_only_hint) : false,
      destructiveHint: isSet(object.destructive_hint) ? globalThis.Boolean(object.destructive_hint) : false,
      idempotentHint: isSet(object.idempotent_hint) ? globalThis.Boolean(object.idempotent_hint) : false,
      openWorldHint: isSet(object.open_world_hint) ? globalThis.Boolean(object.open_world_hint) : false,
      callId: isSet(object.call_id) ? globalThis.String(object.call_id) : "",
      disable: isSet(object.disable) ? globalThis.Boolean(object.disable) : false,
      profiles: globalThis.Array.isArray(object?.profiles) ? object.profiles.map((e: any) => Profile.fromJSON(e)) : [],
      mergeStrategy: isSet(object.merge_strategy) ? toolDefinition_MergeStrategyFromJSON(object.merge_strategy) : 0,
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => globalThis.String(e)) : [],
      integrity: isSet(object.integrity) ? Integrity.fromJSON(object.integrity) : undefined,
    };
  },

  toJSON(message: ToolDefinition): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.inputSchema !== undefined) {
      obj.input_schema = message.inputSchema;
    }
    if (message.serviceId !== "") {
      obj.service_id = message.serviceId;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.isStream !== false) {
      obj.is_stream = message.isStream;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.readOnlyHint !== false) {
      obj.read_only_hint = message.readOnlyHint;
    }
    if (message.destructiveHint !== false) {
      obj.destructive_hint = message.destructiveHint;
    }
    if (message.idempotentHint !== false) {
      obj.idempotent_hint = message.idempotentHint;
    }
    if (message.openWorldHint !== false) {
      obj.open_world_hint = message.openWorldHint;
    }
    if (message.callId !== "") {
      obj.call_id = message.callId;
    }
    if (message.disable !== false) {
      obj.disable = message.disable;
    }
    if (message.profiles?.length) {
      obj.profiles = message.profiles.map((e) => Profile.toJSON(e));
    }
    if (message.mergeStrategy !== 0) {
      obj.merge_strategy = toolDefinition_MergeStrategyToJSON(message.mergeStrategy);
    }
    if (message.tags?.length) {
      obj.tags = message.tags;
    }
    if (message.integrity !== undefined) {
      obj.integrity = Integrity.toJSON(message.integrity);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolDefinition>, I>>(base?: I): ToolDefinition {
    return ToolDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolDefinition>, I>>(object: I): ToolDefinition {
    const message = createBaseToolDefinition();
    message.name = object.name ?? "";
    message.inputSchema = object.inputSchema ?? undefined;
    message.serviceId = object.serviceId ?? "";
    message.description = object.description ?? "";
    message.isStream = object.isStream ?? false;
    message.title = object.title ?? "";
    message.readOnlyHint = object.readOnlyHint ?? false;
    message.destructiveHint = object.destructiveHint ?? false;
    message.idempotentHint = object.idempotentHint ?? false;
    message.openWorldHint = object.openWorldHint ?? false;
    message.callId = object.callId ?? "";
    message.disable = object.disable ?? false;
    message.profiles = object.profiles?.map((e) => Profile.fromPartial(e)) || [];
    message.mergeStrategy = object.mergeStrategy ?? 0;
    message.tags = object.tags?.map((e) => e) || [];
    message.integrity = (object.integrity !== undefined && object.integrity !== null)
      ? Integrity.fromPartial(object.integrity)
      : undefined;
    return message;
  },
};

function createBaseIntegrity(): Integrity {
  return { hash: "", algorithm: "" };
}

export const Integrity: MessageFns<Integrity> = {
  encode(message: Integrity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hash !== "") {
      writer.uint32(10).string(message.hash);
    }
    if (message.algorithm !== "") {
      writer.uint32(18).string(message.algorithm);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Integrity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIntegrity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hash = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.algorithm = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Integrity {
    return {
      hash: isSet(object.hash) ? globalThis.String(object.hash) : "",
      algorithm: isSet(object.algorithm) ? globalThis.String(object.algorithm) : "",
    };
  },

  toJSON(message: Integrity): unknown {
    const obj: any = {};
    if (message.hash !== "") {
      obj.hash = message.hash;
    }
    if (message.algorithm !== "") {
      obj.algorithm = message.algorithm;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Integrity>, I>>(base?: I): Integrity {
    return Integrity.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Integrity>, I>>(object: I): Integrity {
    const message = createBaseIntegrity();
    message.hash = object.hash ?? "";
    message.algorithm = object.algorithm ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
