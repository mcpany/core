// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.1
// source: proto/config/v1/prompt.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Struct } from "../../google/protobuf/struct";
import { Profile } from "./profile";
import { ResourceAnnotation, ResourceDefinition } from "./resource";

export const protobufPackage = "mcpany.config.v1";

/** PromptDefinition defines a prompt that can be used to interact with a language model. */
export interface PromptDefinition {
  /** The name of the prompt, which will be used to invoke it. */
  name: string;
  /** A human-readable title for the prompt. */
  title: string;
  /** A human-readable description of what the prompt does. */
  description: string;
  /** The arguments for the prompt. */
  inputSchema?:
    | { [key: string]: any }
    | undefined;
  /** The messages that make up the prompt. */
  messages: PromptMessage[];
  /** If true, this prompt is disabled. */
  disable: boolean;
  /** A list of profiles this prompt belongs to. */
  profiles: Profile[];
}

/** PromptMessage defines a message in a prompt. */
export interface PromptMessage {
  role: PromptMessage_Role;
  text?: TextContent | undefined;
  image?: ImageContent | undefined;
  audio?: AudioContent | undefined;
  resource?: ResourceContent | undefined;
}

/** The role of the speaker. */
export enum PromptMessage_Role {
  USER = 0,
  ASSISTANT = 1,
  UNRECOGNIZED = -1,
}

export function promptMessage_RoleFromJSON(object: any): PromptMessage_Role {
  switch (object) {
    case 0:
    case "USER":
      return PromptMessage_Role.USER;
    case 1:
    case "ASSISTANT":
      return PromptMessage_Role.ASSISTANT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PromptMessage_Role.UNRECOGNIZED;
  }
}

export function promptMessage_RoleToJSON(object: PromptMessage_Role): string {
  switch (object) {
    case PromptMessage_Role.USER:
      return "USER";
    case PromptMessage_Role.ASSISTANT:
      return "ASSISTANT";
    case PromptMessage_Role.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** TextContent defines a text content block. */
export interface TextContent {
  /** The text content. */
  text: string;
  /** Optional annotations for the content. */
  annotations?: ResourceAnnotation | undefined;
}

/** ImageContent defines an image content block. */
export interface ImageContent {
  /** The base64-encoded image data. */
  data: string;
  /** The MIME type of the image. */
  mimeType: string;
  /** Optional annotations for the content. */
  annotations?: ResourceAnnotation | undefined;
}

/** AudioContent defines an audio content block. */
export interface AudioContent {
  /** The base64-encoded audio data. */
  data: string;
  /** The MIME type of the audio. */
  mimeType: string;
  /** Optional annotations for the content. */
  annotations?: ResourceAnnotation | undefined;
}

/** ResourceContent defines a resource content block. */
export interface ResourceContent {
  /** The resource. */
  resource?:
    | ResourceDefinition
    | undefined;
  /** Optional annotations for the content. */
  annotations?: ResourceAnnotation | undefined;
}

function createBasePromptDefinition(): PromptDefinition {
  return { name: "", title: "", description: "", inputSchema: undefined, messages: [], disable: false, profiles: [] };
}

export const PromptDefinition: MessageFns<PromptDefinition> = {
  encode(message: PromptDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.inputSchema !== undefined) {
      Struct.encode(Struct.wrap(message.inputSchema), writer.uint32(34).fork()).join();
    }
    for (const v of message.messages) {
      PromptMessage.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.disable !== false) {
      writer.uint32(48).bool(message.disable);
    }
    for (const v of message.profiles) {
      Profile.encode(v!, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PromptDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePromptDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.inputSchema = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.messages.push(PromptMessage.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.disable = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.profiles.push(Profile.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PromptDefinition {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      inputSchema: isObject(object.input_schema) ? object.input_schema : undefined,
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => PromptMessage.fromJSON(e))
        : [],
      disable: isSet(object.disable) ? globalThis.Boolean(object.disable) : false,
      profiles: globalThis.Array.isArray(object?.profiles) ? object.profiles.map((e: any) => Profile.fromJSON(e)) : [],
    };
  },

  toJSON(message: PromptDefinition): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.inputSchema !== undefined) {
      obj.input_schema = message.inputSchema;
    }
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => PromptMessage.toJSON(e));
    }
    if (message.disable !== false) {
      obj.disable = message.disable;
    }
    if (message.profiles?.length) {
      obj.profiles = message.profiles.map((e) => Profile.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PromptDefinition>, I>>(base?: I): PromptDefinition {
    return PromptDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PromptDefinition>, I>>(object: I): PromptDefinition {
    const message = createBasePromptDefinition();
    message.name = object.name ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.inputSchema = object.inputSchema ?? undefined;
    message.messages = object.messages?.map((e) => PromptMessage.fromPartial(e)) || [];
    message.disable = object.disable ?? false;
    message.profiles = object.profiles?.map((e) => Profile.fromPartial(e)) || [];
    return message;
  },
};

function createBasePromptMessage(): PromptMessage {
  return { role: 0, text: undefined, image: undefined, audio: undefined, resource: undefined };
}

export const PromptMessage: MessageFns<PromptMessage> = {
  encode(message: PromptMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.role !== 0) {
      writer.uint32(8).int32(message.role);
    }
    if (message.text !== undefined) {
      TextContent.encode(message.text, writer.uint32(18).fork()).join();
    }
    if (message.image !== undefined) {
      ImageContent.encode(message.image, writer.uint32(26).fork()).join();
    }
    if (message.audio !== undefined) {
      AudioContent.encode(message.audio, writer.uint32(34).fork()).join();
    }
    if (message.resource !== undefined) {
      ResourceContent.encode(message.resource, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PromptMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePromptMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.text = TextContent.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.image = ImageContent.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.audio = AudioContent.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.resource = ResourceContent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PromptMessage {
    return {
      role: isSet(object.role) ? promptMessage_RoleFromJSON(object.role) : 0,
      text: isSet(object.text) ? TextContent.fromJSON(object.text) : undefined,
      image: isSet(object.image) ? ImageContent.fromJSON(object.image) : undefined,
      audio: isSet(object.audio) ? AudioContent.fromJSON(object.audio) : undefined,
      resource: isSet(object.resource) ? ResourceContent.fromJSON(object.resource) : undefined,
    };
  },

  toJSON(message: PromptMessage): unknown {
    const obj: any = {};
    if (message.role !== 0) {
      obj.role = promptMessage_RoleToJSON(message.role);
    }
    if (message.text !== undefined) {
      obj.text = TextContent.toJSON(message.text);
    }
    if (message.image !== undefined) {
      obj.image = ImageContent.toJSON(message.image);
    }
    if (message.audio !== undefined) {
      obj.audio = AudioContent.toJSON(message.audio);
    }
    if (message.resource !== undefined) {
      obj.resource = ResourceContent.toJSON(message.resource);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PromptMessage>, I>>(base?: I): PromptMessage {
    return PromptMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PromptMessage>, I>>(object: I): PromptMessage {
    const message = createBasePromptMessage();
    message.role = object.role ?? 0;
    message.text = (object.text !== undefined && object.text !== null)
      ? TextContent.fromPartial(object.text)
      : undefined;
    message.image = (object.image !== undefined && object.image !== null)
      ? ImageContent.fromPartial(object.image)
      : undefined;
    message.audio = (object.audio !== undefined && object.audio !== null)
      ? AudioContent.fromPartial(object.audio)
      : undefined;
    message.resource = (object.resource !== undefined && object.resource !== null)
      ? ResourceContent.fromPartial(object.resource)
      : undefined;
    return message;
  },
};

function createBaseTextContent(): TextContent {
  return { text: "", annotations: undefined };
}

export const TextContent: MessageFns<TextContent> = {
  encode(message: TextContent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.annotations !== undefined) {
      ResourceAnnotation.encode(message.annotations, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TextContent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTextContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.annotations = ResourceAnnotation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TextContent {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      annotations: isSet(object.annotations) ? ResourceAnnotation.fromJSON(object.annotations) : undefined,
    };
  },

  toJSON(message: TextContent): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.annotations !== undefined) {
      obj.annotations = ResourceAnnotation.toJSON(message.annotations);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TextContent>, I>>(base?: I): TextContent {
    return TextContent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TextContent>, I>>(object: I): TextContent {
    const message = createBaseTextContent();
    message.text = object.text ?? "";
    message.annotations = (object.annotations !== undefined && object.annotations !== null)
      ? ResourceAnnotation.fromPartial(object.annotations)
      : undefined;
    return message;
  },
};

function createBaseImageContent(): ImageContent {
  return { data: "", mimeType: "", annotations: undefined };
}

export const ImageContent: MessageFns<ImageContent> = {
  encode(message: ImageContent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== "") {
      writer.uint32(10).string(message.data);
    }
    if (message.mimeType !== "") {
      writer.uint32(18).string(message.mimeType);
    }
    if (message.annotations !== undefined) {
      ResourceAnnotation.encode(message.annotations, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageContent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.annotations = ResourceAnnotation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageContent {
    return {
      data: isSet(object.data) ? globalThis.String(object.data) : "",
      mimeType: isSet(object.mimeType)
        ? globalThis.String(object.mimeType)
        : isSet(object.mime_type)
        ? globalThis.String(object.mime_type)
        : "",
      annotations: isSet(object.annotations) ? ResourceAnnotation.fromJSON(object.annotations) : undefined,
    };
  },

  toJSON(message: ImageContent): unknown {
    const obj: any = {};
    if (message.data !== "") {
      obj.data = message.data;
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    if (message.annotations !== undefined) {
      obj.annotations = ResourceAnnotation.toJSON(message.annotations);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImageContent>, I>>(base?: I): ImageContent {
    return ImageContent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImageContent>, I>>(object: I): ImageContent {
    const message = createBaseImageContent();
    message.data = object.data ?? "";
    message.mimeType = object.mimeType ?? "";
    message.annotations = (object.annotations !== undefined && object.annotations !== null)
      ? ResourceAnnotation.fromPartial(object.annotations)
      : undefined;
    return message;
  },
};

function createBaseAudioContent(): AudioContent {
  return { data: "", mimeType: "", annotations: undefined };
}

export const AudioContent: MessageFns<AudioContent> = {
  encode(message: AudioContent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== "") {
      writer.uint32(10).string(message.data);
    }
    if (message.mimeType !== "") {
      writer.uint32(18).string(message.mimeType);
    }
    if (message.annotations !== undefined) {
      ResourceAnnotation.encode(message.annotations, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AudioContent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAudioContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.annotations = ResourceAnnotation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AudioContent {
    return {
      data: isSet(object.data) ? globalThis.String(object.data) : "",
      mimeType: isSet(object.mimeType)
        ? globalThis.String(object.mimeType)
        : isSet(object.mime_type)
        ? globalThis.String(object.mime_type)
        : "",
      annotations: isSet(object.annotations) ? ResourceAnnotation.fromJSON(object.annotations) : undefined,
    };
  },

  toJSON(message: AudioContent): unknown {
    const obj: any = {};
    if (message.data !== "") {
      obj.data = message.data;
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    if (message.annotations !== undefined) {
      obj.annotations = ResourceAnnotation.toJSON(message.annotations);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AudioContent>, I>>(base?: I): AudioContent {
    return AudioContent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AudioContent>, I>>(object: I): AudioContent {
    const message = createBaseAudioContent();
    message.data = object.data ?? "";
    message.mimeType = object.mimeType ?? "";
    message.annotations = (object.annotations !== undefined && object.annotations !== null)
      ? ResourceAnnotation.fromPartial(object.annotations)
      : undefined;
    return message;
  },
};

function createBaseResourceContent(): ResourceContent {
  return { resource: undefined, annotations: undefined };
}

export const ResourceContent: MessageFns<ResourceContent> = {
  encode(message: ResourceContent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resource !== undefined) {
      ResourceDefinition.encode(message.resource, writer.uint32(10).fork()).join();
    }
    if (message.annotations !== undefined) {
      ResourceAnnotation.encode(message.annotations, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResourceContent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResourceContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.resource = ResourceDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.annotations = ResourceAnnotation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResourceContent {
    return {
      resource: isSet(object.resource) ? ResourceDefinition.fromJSON(object.resource) : undefined,
      annotations: isSet(object.annotations) ? ResourceAnnotation.fromJSON(object.annotations) : undefined,
    };
  },

  toJSON(message: ResourceContent): unknown {
    const obj: any = {};
    if (message.resource !== undefined) {
      obj.resource = ResourceDefinition.toJSON(message.resource);
    }
    if (message.annotations !== undefined) {
      obj.annotations = ResourceAnnotation.toJSON(message.annotations);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResourceContent>, I>>(base?: I): ResourceContent {
    return ResourceContent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResourceContent>, I>>(object: I): ResourceContent {
    const message = createBaseResourceContent();
    message.resource = (object.resource !== undefined && object.resource !== null)
      ? ResourceDefinition.fromPartial(object.resource)
      : undefined;
    message.annotations = (object.annotations !== undefined && object.annotations !== null)
      ? ResourceAnnotation.fromPartial(object.annotations)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
