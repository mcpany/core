// Copyright 2026 Author(s) of MCP Any
// SPDX-License-Identifier: Apache-2.0
// TypeScript declaration stubs for proto/config/v1/tool.proto
// These declarations provide type information for the Bazel build environment.
// The full implementation is generated by ts-proto via `make gen`.

export declare enum ToolDefinition_MergeStrategy {
  MERGE_STRATEGY_UNSPECIFIED = 0,
  MERGE_STRATEGY_MERGE = 1,
  MERGE_STRATEGY_OVERRIDE = 2,
}

export interface ToolDefinition {
  name: string;
  description: string;
  title?: string;
  serviceId: string;
  inputSchema?: Record<string, unknown>;
  isStream?: boolean;
  readOnlyHint?: boolean;
  destructiveHint?: boolean;
  idempotentHint?: boolean;
  openWorldHint?: boolean;
  callId?: string;
  disable?: boolean;
  profiles?: string[];
  tags: string[];
  mergeStrategy?: ToolDefinition_MergeStrategy;
  integrity?: unknown;
}
