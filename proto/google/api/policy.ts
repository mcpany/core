// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.1
// source: google/api/policy.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "google.api";

/**
 * Google API Policy Annotation
 *
 * This message defines a simple API policy annotation that can be used to
 * annotate API request and response message fields with applicable policies.
 * One field may have multiple applicable policies that must all be satisfied
 * before a request can be processed. This policy annotation is used to
 * generate the overall policy that will be used for automatic runtime
 * policy enforcement and documentation generation.
 */
export interface FieldPolicy {
  /**
   * Selects one or more request or response message fields to apply this
   * `FieldPolicy`.
   *
   * When a `FieldPolicy` is used in proto annotation, the selector must
   * be left as empty. The service config generator will automatically fill
   * the correct value.
   *
   * When a `FieldPolicy` is used in service config, the selector must be a
   * comma-separated string with valid request or response field paths,
   * such as "foo.bar" or "foo.bar,foo.baz".
   */
  selector: string;
  /**
   * Specifies the required permission(s) for the resource referred to by the
   * field. It requires the field contains a valid resource reference, and
   * the request must pass the permission checks to proceed. For example,
   * "resourcemanager.projects.get".
   */
  resourcePermission: string;
  /** Specifies the resource type for the resource referred to by the field. */
  resourceType: string;
}

/** Defines policies applying to an RPC method. */
export interface MethodPolicy {
  /**
   * Selects a method to which these policies should be enforced, for example,
   * "google.pubsub.v1.Subscriber.CreateSubscription".
   *
   * Refer to [selector][google.api.DocumentationRule.selector] for syntax
   * details.
   *
   * NOTE: This field must not be set in the proto annotation. It will be
   * automatically filled by the service config compiler .
   */
  selector: string;
  /** Policies that are applicable to the request message. */
  requestPolicies: FieldPolicy[];
}

function createBaseFieldPolicy(): FieldPolicy {
  return { selector: "", resourcePermission: "", resourceType: "" };
}

export const FieldPolicy: MessageFns<FieldPolicy> = {
  encode(message: FieldPolicy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.selector !== "") {
      writer.uint32(10).string(message.selector);
    }
    if (message.resourcePermission !== "") {
      writer.uint32(18).string(message.resourcePermission);
    }
    if (message.resourceType !== "") {
      writer.uint32(26).string(message.resourceType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FieldPolicy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFieldPolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.selector = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.resourcePermission = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.resourceType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FieldPolicy {
    return {
      selector: isSet(object.selector) ? globalThis.String(object.selector) : "",
      resourcePermission: isSet(object.resourcePermission)
        ? globalThis.String(object.resourcePermission)
        : isSet(object.resource_permission)
        ? globalThis.String(object.resource_permission)
        : "",
      resourceType: isSet(object.resourceType)
        ? globalThis.String(object.resourceType)
        : isSet(object.resource_type)
        ? globalThis.String(object.resource_type)
        : "",
    };
  },

  toJSON(message: FieldPolicy): unknown {
    const obj: any = {};
    if (message.selector !== "") {
      obj.selector = message.selector;
    }
    if (message.resourcePermission !== "") {
      obj.resourcePermission = message.resourcePermission;
    }
    if (message.resourceType !== "") {
      obj.resourceType = message.resourceType;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FieldPolicy>, I>>(base?: I): FieldPolicy {
    return FieldPolicy.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FieldPolicy>, I>>(object: I): FieldPolicy {
    const message = createBaseFieldPolicy();
    message.selector = object.selector ?? "";
    message.resourcePermission = object.resourcePermission ?? "";
    message.resourceType = object.resourceType ?? "";
    return message;
  },
};

function createBaseMethodPolicy(): MethodPolicy {
  return { selector: "", requestPolicies: [] };
}

export const MethodPolicy: MessageFns<MethodPolicy> = {
  encode(message: MethodPolicy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.selector !== "") {
      writer.uint32(74).string(message.selector);
    }
    for (const v of message.requestPolicies) {
      FieldPolicy.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MethodPolicy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMethodPolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.selector = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestPolicies.push(FieldPolicy.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MethodPolicy {
    return {
      selector: isSet(object.selector) ? globalThis.String(object.selector) : "",
      requestPolicies: globalThis.Array.isArray(object?.requestPolicies)
        ? object.requestPolicies.map((e: any) => FieldPolicy.fromJSON(e))
        : globalThis.Array.isArray(object?.request_policies)
        ? object.request_policies.map((e: any) => FieldPolicy.fromJSON(e))
        : [],
    };
  },

  toJSON(message: MethodPolicy): unknown {
    const obj: any = {};
    if (message.selector !== "") {
      obj.selector = message.selector;
    }
    if (message.requestPolicies?.length) {
      obj.requestPolicies = message.requestPolicies.map((e) => FieldPolicy.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MethodPolicy>, I>>(base?: I): MethodPolicy {
    return MethodPolicy.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MethodPolicy>, I>>(object: I): MethodPolicy {
    const message = createBaseMethodPolicy();
    message.selector = object.selector ?? "";
    message.requestPolicies = object.requestPolicies?.map((e) => FieldPolicy.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
