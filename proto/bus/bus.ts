// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.1
// source: proto/bus/bus.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "bus";

export interface MessageBus {
  inMemory?: InMemoryBus | undefined;
  redis?: RedisBus | undefined;
  nats?: NatsBus | undefined;
  kafka?: KafkaBus | undefined;
}

export interface InMemoryBus {
}

export interface NatsBus {
  serverUrl: string;
}

export interface RedisBus {
  address: string;
  db: number;
  password: string;
}

export interface KafkaBus {
  brokers: string[];
  topicPrefix: string;
  consumerGroup: string;
}

function createBaseMessageBus(): MessageBus {
  return { inMemory: undefined, redis: undefined, nats: undefined, kafka: undefined };
}

export const MessageBus: MessageFns<MessageBus> = {
  encode(message: MessageBus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.inMemory !== undefined) {
      InMemoryBus.encode(message.inMemory, writer.uint32(10).fork()).join();
    }
    if (message.redis !== undefined) {
      RedisBus.encode(message.redis, writer.uint32(18).fork()).join();
    }
    if (message.nats !== undefined) {
      NatsBus.encode(message.nats, writer.uint32(26).fork()).join();
    }
    if (message.kafka !== undefined) {
      KafkaBus.encode(message.kafka, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessageBus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessageBus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.inMemory = InMemoryBus.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.redis = RedisBus.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nats = NatsBus.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.kafka = KafkaBus.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MessageBus {
    return {
      inMemory: isSet(object.inMemory)
        ? InMemoryBus.fromJSON(object.inMemory)
        : isSet(object.in_memory)
        ? InMemoryBus.fromJSON(object.in_memory)
        : undefined,
      redis: isSet(object.redis) ? RedisBus.fromJSON(object.redis) : undefined,
      nats: isSet(object.nats) ? NatsBus.fromJSON(object.nats) : undefined,
      kafka: isSet(object.kafka) ? KafkaBus.fromJSON(object.kafka) : undefined,
    };
  },

  toJSON(message: MessageBus): unknown {
    const obj: any = {};
    if (message.inMemory !== undefined) {
      obj.inMemory = InMemoryBus.toJSON(message.inMemory);
    }
    if (message.redis !== undefined) {
      obj.redis = RedisBus.toJSON(message.redis);
    }
    if (message.nats !== undefined) {
      obj.nats = NatsBus.toJSON(message.nats);
    }
    if (message.kafka !== undefined) {
      obj.kafka = KafkaBus.toJSON(message.kafka);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MessageBus>, I>>(base?: I): MessageBus {
    return MessageBus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MessageBus>, I>>(object: I): MessageBus {
    const message = createBaseMessageBus();
    message.inMemory = (object.inMemory !== undefined && object.inMemory !== null)
      ? InMemoryBus.fromPartial(object.inMemory)
      : undefined;
    message.redis = (object.redis !== undefined && object.redis !== null)
      ? RedisBus.fromPartial(object.redis)
      : undefined;
    message.nats = (object.nats !== undefined && object.nats !== null) ? NatsBus.fromPartial(object.nats) : undefined;
    message.kafka = (object.kafka !== undefined && object.kafka !== null)
      ? KafkaBus.fromPartial(object.kafka)
      : undefined;
    return message;
  },
};

function createBaseInMemoryBus(): InMemoryBus {
  return {};
}

export const InMemoryBus: MessageFns<InMemoryBus> = {
  encode(_: InMemoryBus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InMemoryBus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInMemoryBus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): InMemoryBus {
    return {};
  },

  toJSON(_: InMemoryBus): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<InMemoryBus>, I>>(base?: I): InMemoryBus {
    return InMemoryBus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InMemoryBus>, I>>(_: I): InMemoryBus {
    const message = createBaseInMemoryBus();
    return message;
  },
};

function createBaseNatsBus(): NatsBus {
  return { serverUrl: "" };
}

export const NatsBus: MessageFns<NatsBus> = {
  encode(message: NatsBus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverUrl !== "") {
      writer.uint32(10).string(message.serverUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NatsBus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNatsBus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NatsBus {
    return {
      serverUrl: isSet(object.serverUrl)
        ? globalThis.String(object.serverUrl)
        : isSet(object.server_url)
        ? globalThis.String(object.server_url)
        : "",
    };
  },

  toJSON(message: NatsBus): unknown {
    const obj: any = {};
    if (message.serverUrl !== "") {
      obj.serverUrl = message.serverUrl;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NatsBus>, I>>(base?: I): NatsBus {
    return NatsBus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NatsBus>, I>>(object: I): NatsBus {
    const message = createBaseNatsBus();
    message.serverUrl = object.serverUrl ?? "";
    return message;
  },
};

function createBaseRedisBus(): RedisBus {
  return { address: "", db: 0, password: "" };
}

export const RedisBus: MessageFns<RedisBus> = {
  encode(message: RedisBus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.db !== 0) {
      writer.uint32(16).int32(message.db);
    }
    if (message.password !== "") {
      writer.uint32(26).string(message.password);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RedisBus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRedisBus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.db = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.password = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RedisBus {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      db: isSet(object.db) ? globalThis.Number(object.db) : 0,
      password: isSet(object.password) ? globalThis.String(object.password) : "",
    };
  },

  toJSON(message: RedisBus): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.db !== 0) {
      obj.db = Math.round(message.db);
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RedisBus>, I>>(base?: I): RedisBus {
    return RedisBus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RedisBus>, I>>(object: I): RedisBus {
    const message = createBaseRedisBus();
    message.address = object.address ?? "";
    message.db = object.db ?? 0;
    message.password = object.password ?? "";
    return message;
  },
};

function createBaseKafkaBus(): KafkaBus {
  return { brokers: [], topicPrefix: "", consumerGroup: "" };
}

export const KafkaBus: MessageFns<KafkaBus> = {
  encode(message: KafkaBus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.brokers) {
      writer.uint32(10).string(v!);
    }
    if (message.topicPrefix !== "") {
      writer.uint32(18).string(message.topicPrefix);
    }
    if (message.consumerGroup !== "") {
      writer.uint32(26).string(message.consumerGroup);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KafkaBus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKafkaBus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.brokers.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.topicPrefix = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.consumerGroup = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KafkaBus {
    return {
      brokers: globalThis.Array.isArray(object?.brokers) ? object.brokers.map((e: any) => globalThis.String(e)) : [],
      topicPrefix: isSet(object.topicPrefix)
        ? globalThis.String(object.topicPrefix)
        : isSet(object.topic_prefix)
        ? globalThis.String(object.topic_prefix)
        : "",
      consumerGroup: isSet(object.consumerGroup)
        ? globalThis.String(object.consumerGroup)
        : isSet(object.consumer_group)
        ? globalThis.String(object.consumer_group)
        : "",
    };
  },

  toJSON(message: KafkaBus): unknown {
    const obj: any = {};
    if (message.brokers?.length) {
      obj.brokers = message.brokers;
    }
    if (message.topicPrefix !== "") {
      obj.topicPrefix = message.topicPrefix;
    }
    if (message.consumerGroup !== "") {
      obj.consumerGroup = message.consumerGroup;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KafkaBus>, I>>(base?: I): KafkaBus {
    return KafkaBus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KafkaBus>, I>>(object: I): KafkaBus {
    const message = createBaseKafkaBus();
    message.brokers = object.brokers?.map((e) => e) || [];
    message.topicPrefix = object.topicPrefix ?? "";
    message.consumerGroup = object.consumerGroup ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
