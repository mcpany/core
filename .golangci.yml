# Copyright 2025 Author(s) of MCP Any
# SPDX-License-Identifier: Apache-2.0

version: "2"

run:
  timeout: 5m
  tests: false
  skip-dirs:
    - build
    - vendor
    - .audit

linters:
  enable:
    - bodyclose
    - errcheck
    - goconst
    - gocritic
    - gocyclo
    - govet
    - nolintlint
    - prealloc
    - revive
    - gosec
    - staticcheck
    - tparallel
    - unconvert
    - unused

issues:
  exclude-dirs:
    - build
    - vendor
    - .audit
    - .cache
  exclude-rules:
    - path: "_test.go"
      linters:
        - "errcheck"
  skip-files:
    - '.*\.pb\.go$'
