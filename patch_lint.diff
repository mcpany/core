--- /app/server/tests/integration/e2e_helpers.go
+++ /app/server/tests/integration/e2e_helpers.go
@@ -224,7 +224,7 @@

 func runfilesRoots() []string {
	workspace := runfilesWorkspaceName()
-	var roots []string
+	roots := make([]string, 0, 2)
	for _, base := range []string{os.Getenv("TEST_SRCDIR"), os.Getenv("RUNFILES_DIR")} {
		if base == "" {
			continue
@@ -270,7 +270,7 @@

 func symlinkIfPresent(src, dst string) error {
	if _, err := os.Stat(src); err != nil {
-		return nil
+		return nil //nolint:nilerr // Ignore error to proceed silently if missing
	}
	return os.Symlink(src, dst)
 }
