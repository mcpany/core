--- server/pkg/app/api_traces_test.go
+++ server/pkg/app/api_traces_test.go
@@ -52,6 +52,7 @@
	// Initialize Application
	app := &Application{
		standardMiddlewares: standardMiddlewares,
+		seededTraceSubs:     make(map[chan *Trace]struct{}),
	}

	return app, auditMiddleware
