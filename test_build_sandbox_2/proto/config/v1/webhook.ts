// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.1
// source: proto/config/v1/webhook.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Duration } from "../../google/protobuf/duration";
import { Struct } from "../../google/protobuf/struct";

export const protobufPackage = "mcpany.config.v1";

export enum WebhookKind {
  WEBHOOK_KIND_UNSPECIFIED = 0,
  WEBHOOK_KIND_PRE_CALL = 1,
  WEBHOOK_KIND_POST_CALL = 2,
  WEBHOOK_KIND_TRANSFORM_INPUT = 3,
  UNRECOGNIZED = -1,
}

export function webhookKindFromJSON(object: any): WebhookKind {
  switch (object) {
    case 0:
    case "WEBHOOK_KIND_UNSPECIFIED":
      return WebhookKind.WEBHOOK_KIND_UNSPECIFIED;
    case 1:
    case "WEBHOOK_KIND_PRE_CALL":
      return WebhookKind.WEBHOOK_KIND_PRE_CALL;
    case 2:
    case "WEBHOOK_KIND_POST_CALL":
      return WebhookKind.WEBHOOK_KIND_POST_CALL;
    case 3:
    case "WEBHOOK_KIND_TRANSFORM_INPUT":
      return WebhookKind.WEBHOOK_KIND_TRANSFORM_INPUT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return WebhookKind.UNRECOGNIZED;
  }
}

export function webhookKindToJSON(object: WebhookKind): string {
  switch (object) {
    case WebhookKind.WEBHOOK_KIND_UNSPECIFIED:
      return "WEBHOOK_KIND_UNSPECIFIED";
    case WebhookKind.WEBHOOK_KIND_PRE_CALL:
      return "WEBHOOK_KIND_PRE_CALL";
    case WebhookKind.WEBHOOK_KIND_POST_CALL:
      return "WEBHOOK_KIND_POST_CALL";
    case WebhookKind.WEBHOOK_KIND_TRANSFORM_INPUT:
      return "WEBHOOK_KIND_TRANSFORM_INPUT";
    case WebhookKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** WebhookReview explains the request and response of a webhook call. */
export interface WebhookReview {
  request?: WebhookRequest | undefined;
  response?: WebhookResponse | undefined;
}

export interface WebhookRequest {
  /** Unique identifier for the request. */
  uid: string;
  /** The operation type (e.g., "PreCall", "PostCall"). */
  kind: WebhookKind;
  /** The tool name being executed. */
  toolName: string;
  /** The object being reviewed (e.g., ToolInputs for PreCall, ToolResult for PostCall). */
  object?: { [key: string]: any } | undefined;
}

export interface WebhookConfig {
  url: string;
  timeout?: Duration | undefined;
  webhookSecret: string;
}

export interface SystemWebhookConfig {
  name: string;
  description: string;
  urlPath: string;
  disabled: boolean;
}

export interface WebhookResponse {
  /** Unique identifier for the request. */
  uid: string;
  /** allowed indicates if the request is permitted (for PreCall). */
  allowed: boolean;
  /** status contains extra details for the decision. */
  status?:
    | WebhookStatus
    | undefined;
  /** replacement_object is a complete replacement for the object (simple mutation). */
  replacementObject?: { [key: string]: any } | undefined;
}

export interface WebhookStatus {
  code: number;
  message: string;
}

function createBaseWebhookReview(): WebhookReview {
  return { request: undefined, response: undefined };
}

export const WebhookReview: MessageFns<WebhookReview> = {
  encode(message: WebhookReview, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.request !== undefined) {
      WebhookRequest.encode(message.request, writer.uint32(10).fork()).join();
    }
    if (message.response !== undefined) {
      WebhookResponse.encode(message.response, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WebhookReview {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWebhookReview();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.request = WebhookRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.response = WebhookResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WebhookReview {
    return {
      request: isSet(object.request) ? WebhookRequest.fromJSON(object.request) : undefined,
      response: isSet(object.response) ? WebhookResponse.fromJSON(object.response) : undefined,
    };
  },

  toJSON(message: WebhookReview): unknown {
    const obj: any = {};
    if (message.request !== undefined) {
      obj.request = WebhookRequest.toJSON(message.request);
    }
    if (message.response !== undefined) {
      obj.response = WebhookResponse.toJSON(message.response);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WebhookReview>, I>>(base?: I): WebhookReview {
    return WebhookReview.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WebhookReview>, I>>(object: I): WebhookReview {
    const message = createBaseWebhookReview();
    message.request = (object.request !== undefined && object.request !== null)
      ? WebhookRequest.fromPartial(object.request)
      : undefined;
    message.response = (object.response !== undefined && object.response !== null)
      ? WebhookResponse.fromPartial(object.response)
      : undefined;
    return message;
  },
};

function createBaseWebhookRequest(): WebhookRequest {
  return { uid: "", kind: 0, toolName: "", object: undefined };
}

export const WebhookRequest: MessageFns<WebhookRequest> = {
  encode(message: WebhookRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uid !== "") {
      writer.uint32(10).string(message.uid);
    }
    if (message.kind !== 0) {
      writer.uint32(16).int32(message.kind);
    }
    if (message.toolName !== "") {
      writer.uint32(26).string(message.toolName);
    }
    if (message.object !== undefined) {
      Struct.encode(Struct.wrap(message.object), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WebhookRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWebhookRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.toolName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.object = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WebhookRequest {
    return {
      uid: isSet(object.uid) ? globalThis.String(object.uid) : "",
      kind: isSet(object.kind) ? webhookKindFromJSON(object.kind) : 0,
      toolName: isSet(object.toolName)
        ? globalThis.String(object.toolName)
        : isSet(object.tool_name)
        ? globalThis.String(object.tool_name)
        : "",
      object: isObject(object.object) ? object.object : undefined,
    };
  },

  toJSON(message: WebhookRequest): unknown {
    const obj: any = {};
    if (message.uid !== "") {
      obj.uid = message.uid;
    }
    if (message.kind !== 0) {
      obj.kind = webhookKindToJSON(message.kind);
    }
    if (message.toolName !== "") {
      obj.toolName = message.toolName;
    }
    if (message.object !== undefined) {
      obj.object = message.object;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WebhookRequest>, I>>(base?: I): WebhookRequest {
    return WebhookRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WebhookRequest>, I>>(object: I): WebhookRequest {
    const message = createBaseWebhookRequest();
    message.uid = object.uid ?? "";
    message.kind = object.kind ?? 0;
    message.toolName = object.toolName ?? "";
    message.object = object.object ?? undefined;
    return message;
  },
};

function createBaseWebhookConfig(): WebhookConfig {
  return { url: "", timeout: undefined, webhookSecret: "" };
}

export const WebhookConfig: MessageFns<WebhookConfig> = {
  encode(message: WebhookConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    if (message.timeout !== undefined) {
      Duration.encode(message.timeout, writer.uint32(18).fork()).join();
    }
    if (message.webhookSecret !== "") {
      writer.uint32(26).string(message.webhookSecret);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WebhookConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWebhookConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.webhookSecret = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WebhookConfig {
    return {
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      timeout: isSet(object.timeout) ? Duration.fromJSON(object.timeout) : undefined,
      webhookSecret: isSet(object.webhookSecret)
        ? globalThis.String(object.webhookSecret)
        : isSet(object.webhook_secret)
        ? globalThis.String(object.webhook_secret)
        : "",
    };
  },

  toJSON(message: WebhookConfig): unknown {
    const obj: any = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.timeout !== undefined) {
      obj.timeout = Duration.toJSON(message.timeout);
    }
    if (message.webhookSecret !== "") {
      obj.webhookSecret = message.webhookSecret;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WebhookConfig>, I>>(base?: I): WebhookConfig {
    return WebhookConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WebhookConfig>, I>>(object: I): WebhookConfig {
    const message = createBaseWebhookConfig();
    message.url = object.url ?? "";
    message.timeout = (object.timeout !== undefined && object.timeout !== null)
      ? Duration.fromPartial(object.timeout)
      : undefined;
    message.webhookSecret = object.webhookSecret ?? "";
    return message;
  },
};

function createBaseSystemWebhookConfig(): SystemWebhookConfig {
  return { name: "", description: "", urlPath: "", disabled: false };
}

export const SystemWebhookConfig: MessageFns<SystemWebhookConfig> = {
  encode(message: SystemWebhookConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.urlPath !== "") {
      writer.uint32(26).string(message.urlPath);
    }
    if (message.disabled !== false) {
      writer.uint32(32).bool(message.disabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SystemWebhookConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSystemWebhookConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.urlPath = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.disabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SystemWebhookConfig {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      urlPath: isSet(object.urlPath)
        ? globalThis.String(object.urlPath)
        : isSet(object.url_path)
        ? globalThis.String(object.url_path)
        : "",
      disabled: isSet(object.disabled) ? globalThis.Boolean(object.disabled) : false,
    };
  },

  toJSON(message: SystemWebhookConfig): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.urlPath !== "") {
      obj.urlPath = message.urlPath;
    }
    if (message.disabled !== false) {
      obj.disabled = message.disabled;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SystemWebhookConfig>, I>>(base?: I): SystemWebhookConfig {
    return SystemWebhookConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SystemWebhookConfig>, I>>(object: I): SystemWebhookConfig {
    const message = createBaseSystemWebhookConfig();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.urlPath = object.urlPath ?? "";
    message.disabled = object.disabled ?? false;
    return message;
  },
};

function createBaseWebhookResponse(): WebhookResponse {
  return { uid: "", allowed: false, status: undefined, replacementObject: undefined };
}

export const WebhookResponse: MessageFns<WebhookResponse> = {
  encode(message: WebhookResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uid !== "") {
      writer.uint32(10).string(message.uid);
    }
    if (message.allowed !== false) {
      writer.uint32(16).bool(message.allowed);
    }
    if (message.status !== undefined) {
      WebhookStatus.encode(message.status, writer.uint32(26).fork()).join();
    }
    if (message.replacementObject !== undefined) {
      Struct.encode(Struct.wrap(message.replacementObject), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WebhookResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWebhookResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.allowed = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.status = WebhookStatus.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.replacementObject = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WebhookResponse {
    return {
      uid: isSet(object.uid) ? globalThis.String(object.uid) : "",
      allowed: isSet(object.allowed) ? globalThis.Boolean(object.allowed) : false,
      status: isSet(object.status) ? WebhookStatus.fromJSON(object.status) : undefined,
      replacementObject: isObject(object.replacementObject)
        ? object.replacementObject
        : isObject(object.replacement_object)
        ? object.replacement_object
        : undefined,
    };
  },

  toJSON(message: WebhookResponse): unknown {
    const obj: any = {};
    if (message.uid !== "") {
      obj.uid = message.uid;
    }
    if (message.allowed !== false) {
      obj.allowed = message.allowed;
    }
    if (message.status !== undefined) {
      obj.status = WebhookStatus.toJSON(message.status);
    }
    if (message.replacementObject !== undefined) {
      obj.replacementObject = message.replacementObject;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WebhookResponse>, I>>(base?: I): WebhookResponse {
    return WebhookResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WebhookResponse>, I>>(object: I): WebhookResponse {
    const message = createBaseWebhookResponse();
    message.uid = object.uid ?? "";
    message.allowed = object.allowed ?? false;
    message.status = (object.status !== undefined && object.status !== null)
      ? WebhookStatus.fromPartial(object.status)
      : undefined;
    message.replacementObject = object.replacementObject ?? undefined;
    return message;
  },
};

function createBaseWebhookStatus(): WebhookStatus {
  return { code: 0, message: "" };
}

export const WebhookStatus: MessageFns<WebhookStatus> = {
  encode(message: WebhookStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WebhookStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWebhookStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WebhookStatus {
    return {
      code: isSet(object.code) ? globalThis.Number(object.code) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: WebhookStatus): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = Math.round(message.code);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WebhookStatus>, I>>(base?: I): WebhookStatus {
    return WebhookStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WebhookStatus>, I>>(object: I): WebhookStatus {
    const message = createBaseWebhookStatus();
    message.code = object.code ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
