# Selective exclusion for efficient builds
# Required to copy full 'server/' directory without including massive dependencies

# Globally ignore node_modules and build artifacts
**/node_modules
build/
.git/
.DS_Store

# Explicitly ignore the massive upstream integration test artifacts
server/tests/integration/upstream
