--- ui/tests/diff-feature.spec.ts
+++ ui/tests/diff-feature.spec.ts
@@ -34,7 +34,10 @@
         }
       }
     });
+    if (!response.ok()) {
+      console.log(await response.text());
+    }
     expect(response.ok()).toBeTruthy();
   });
