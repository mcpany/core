// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.1
// source: proto/topology/v1/topology.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "topology.v1";

export enum NodeType {
  NODE_TYPE_UNSPECIFIED = 0,
  NODE_TYPE_CLIENT = 1,
  NODE_TYPE_CORE = 2,
  NODE_TYPE_SERVICE = 3,
  NODE_TYPE_TOOL = 4,
  NODE_TYPE_RESOURCE = 5,
  NODE_TYPE_PROMPT = 6,
  NODE_TYPE_API_CALL = 7,
  NODE_TYPE_MIDDLEWARE = 8,
  NODE_TYPE_WEBHOOK = 9,
  UNRECOGNIZED = -1,
}

export function nodeTypeFromJSON(object: any): NodeType {
  switch (object) {
    case 0:
    case "NODE_TYPE_UNSPECIFIED":
      return NodeType.NODE_TYPE_UNSPECIFIED;
    case 1:
    case "NODE_TYPE_CLIENT":
      return NodeType.NODE_TYPE_CLIENT;
    case 2:
    case "NODE_TYPE_CORE":
      return NodeType.NODE_TYPE_CORE;
    case 3:
    case "NODE_TYPE_SERVICE":
      return NodeType.NODE_TYPE_SERVICE;
    case 4:
    case "NODE_TYPE_TOOL":
      return NodeType.NODE_TYPE_TOOL;
    case 5:
    case "NODE_TYPE_RESOURCE":
      return NodeType.NODE_TYPE_RESOURCE;
    case 6:
    case "NODE_TYPE_PROMPT":
      return NodeType.NODE_TYPE_PROMPT;
    case 7:
    case "NODE_TYPE_API_CALL":
      return NodeType.NODE_TYPE_API_CALL;
    case 8:
    case "NODE_TYPE_MIDDLEWARE":
      return NodeType.NODE_TYPE_MIDDLEWARE;
    case 9:
    case "NODE_TYPE_WEBHOOK":
      return NodeType.NODE_TYPE_WEBHOOK;
    case -1:
    case "UNRECOGNIZED":
    default:
      return NodeType.UNRECOGNIZED;
  }
}

export function nodeTypeToJSON(object: NodeType): string {
  switch (object) {
    case NodeType.NODE_TYPE_UNSPECIFIED:
      return "NODE_TYPE_UNSPECIFIED";
    case NodeType.NODE_TYPE_CLIENT:
      return "NODE_TYPE_CLIENT";
    case NodeType.NODE_TYPE_CORE:
      return "NODE_TYPE_CORE";
    case NodeType.NODE_TYPE_SERVICE:
      return "NODE_TYPE_SERVICE";
    case NodeType.NODE_TYPE_TOOL:
      return "NODE_TYPE_TOOL";
    case NodeType.NODE_TYPE_RESOURCE:
      return "NODE_TYPE_RESOURCE";
    case NodeType.NODE_TYPE_PROMPT:
      return "NODE_TYPE_PROMPT";
    case NodeType.NODE_TYPE_API_CALL:
      return "NODE_TYPE_API_CALL";
    case NodeType.NODE_TYPE_MIDDLEWARE:
      return "NODE_TYPE_MIDDLEWARE";
    case NodeType.NODE_TYPE_WEBHOOK:
      return "NODE_TYPE_WEBHOOK";
    case NodeType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum NodeStatus {
  NODE_STATUS_UNSPECIFIED = 0,
  NODE_STATUS_ACTIVE = 1,
  NODE_STATUS_INACTIVE = 2,
  NODE_STATUS_ERROR = 3,
  UNRECOGNIZED = -1,
}

export function nodeStatusFromJSON(object: any): NodeStatus {
  switch (object) {
    case 0:
    case "NODE_STATUS_UNSPECIFIED":
      return NodeStatus.NODE_STATUS_UNSPECIFIED;
    case 1:
    case "NODE_STATUS_ACTIVE":
      return NodeStatus.NODE_STATUS_ACTIVE;
    case 2:
    case "NODE_STATUS_INACTIVE":
      return NodeStatus.NODE_STATUS_INACTIVE;
    case 3:
    case "NODE_STATUS_ERROR":
      return NodeStatus.NODE_STATUS_ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return NodeStatus.UNRECOGNIZED;
  }
}

export function nodeStatusToJSON(object: NodeStatus): string {
  switch (object) {
    case NodeStatus.NODE_STATUS_UNSPECIFIED:
      return "NODE_STATUS_UNSPECIFIED";
    case NodeStatus.NODE_STATUS_ACTIVE:
      return "NODE_STATUS_ACTIVE";
    case NodeStatus.NODE_STATUS_INACTIVE:
      return "NODE_STATUS_INACTIVE";
    case NodeStatus.NODE_STATUS_ERROR:
      return "NODE_STATUS_ERROR";
    case NodeStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Graph represents the full network topology. */
export interface Graph {
  clients: Node[];
  core?: Node | undefined;
}

/** Node represents a node in the topology graph. */
export interface Node {
  id: string;
  label: string;
  type: NodeType;
  status: NodeStatus;
  metadata: { [key: string]: string };
  children: Node[];
  metrics?: NodeMetrics | undefined;
}

export interface Node_MetadataEntry {
  key: string;
  value: string;
}

export interface NodeMetrics {
  qps: number;
  latencyMs: number;
  errorRate: number;
}

function createBaseGraph(): Graph {
  return { clients: [], core: undefined };
}

export const Graph: MessageFns<Graph> = {
  encode(message: Graph, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.clients) {
      Node.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.core !== undefined) {
      Node.encode(message.core, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Graph {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGraph();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.clients.push(Node.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.core = Node.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Graph {
    return {
      clients: globalThis.Array.isArray(object?.clients) ? object.clients.map((e: any) => Node.fromJSON(e)) : [],
      core: isSet(object.core) ? Node.fromJSON(object.core) : undefined,
    };
  },

  toJSON(message: Graph): unknown {
    const obj: any = {};
    if (message.clients?.length) {
      obj.clients = message.clients.map((e) => Node.toJSON(e));
    }
    if (message.core !== undefined) {
      obj.core = Node.toJSON(message.core);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Graph>, I>>(base?: I): Graph {
    return Graph.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Graph>, I>>(object: I): Graph {
    const message = createBaseGraph();
    message.clients = object.clients?.map((e) => Node.fromPartial(e)) || [];
    message.core = (object.core !== undefined && object.core !== null) ? Node.fromPartial(object.core) : undefined;
    return message;
  },
};

function createBaseNode(): Node {
  return { id: "", label: "", type: 0, status: 0, metadata: {}, children: [], metrics: undefined };
}

export const Node: MessageFns<Node> = {
  encode(message: Node, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.label !== "") {
      writer.uint32(18).string(message.label);
    }
    if (message.type !== 0) {
      writer.uint32(24).int32(message.type);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      Node_MetadataEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    for (const v of message.children) {
      Node.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.metrics !== undefined) {
      NodeMetrics.encode(message.metrics, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Node {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.label = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = Node_MetadataEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.metadata[entry5.key] = entry5.value;
          }
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.children.push(Node.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.metrics = NodeMetrics.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Node {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      label: isSet(object.label) ? globalThis.String(object.label) : "",
      type: isSet(object.type) ? nodeTypeFromJSON(object.type) : 0,
      status: isSet(object.status) ? nodeStatusFromJSON(object.status) : 0,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      children: globalThis.Array.isArray(object?.children) ? object.children.map((e: any) => Node.fromJSON(e)) : [],
      metrics: isSet(object.metrics) ? NodeMetrics.fromJSON(object.metrics) : undefined,
    };
  },

  toJSON(message: Node): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.label !== "") {
      obj.label = message.label;
    }
    if (message.type !== 0) {
      obj.type = nodeTypeToJSON(message.type);
    }
    if (message.status !== 0) {
      obj.status = nodeStatusToJSON(message.status);
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    if (message.children?.length) {
      obj.children = message.children.map((e) => Node.toJSON(e));
    }
    if (message.metrics !== undefined) {
      obj.metrics = NodeMetrics.toJSON(message.metrics);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Node>, I>>(base?: I): Node {
    return Node.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Node>, I>>(object: I): Node {
    const message = createBaseNode();
    message.id = object.id ?? "";
    message.label = object.label ?? "";
    message.type = object.type ?? 0;
    message.status = object.status ?? 0;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.children = object.children?.map((e) => Node.fromPartial(e)) || [];
    message.metrics = (object.metrics !== undefined && object.metrics !== null)
      ? NodeMetrics.fromPartial(object.metrics)
      : undefined;
    return message;
  },
};

function createBaseNode_MetadataEntry(): Node_MetadataEntry {
  return { key: "", value: "" };
}

export const Node_MetadataEntry: MessageFns<Node_MetadataEntry> = {
  encode(message: Node_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Node_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNode_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Node_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Node_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Node_MetadataEntry>, I>>(base?: I): Node_MetadataEntry {
    return Node_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Node_MetadataEntry>, I>>(object: I): Node_MetadataEntry {
    const message = createBaseNode_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseNodeMetrics(): NodeMetrics {
  return { qps: 0, latencyMs: 0, errorRate: 0 };
}

export const NodeMetrics: MessageFns<NodeMetrics> = {
  encode(message: NodeMetrics, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.qps !== 0) {
      writer.uint32(9).double(message.qps);
    }
    if (message.latencyMs !== 0) {
      writer.uint32(17).double(message.latencyMs);
    }
    if (message.errorRate !== 0) {
      writer.uint32(25).double(message.errorRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NodeMetrics {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNodeMetrics();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.qps = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.latencyMs = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.errorRate = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NodeMetrics {
    return {
      qps: isSet(object.qps) ? globalThis.Number(object.qps) : 0,
      latencyMs: isSet(object.latencyMs)
        ? globalThis.Number(object.latencyMs)
        : isSet(object.latency_ms)
        ? globalThis.Number(object.latency_ms)
        : 0,
      errorRate: isSet(object.errorRate)
        ? globalThis.Number(object.errorRate)
        : isSet(object.error_rate)
        ? globalThis.Number(object.error_rate)
        : 0,
    };
  },

  toJSON(message: NodeMetrics): unknown {
    const obj: any = {};
    if (message.qps !== 0) {
      obj.qps = message.qps;
    }
    if (message.latencyMs !== 0) {
      obj.latencyMs = message.latencyMs;
    }
    if (message.errorRate !== 0) {
      obj.errorRate = message.errorRate;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NodeMetrics>, I>>(base?: I): NodeMetrics {
    return NodeMetrics.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NodeMetrics>, I>>(object: I): NodeMetrics {
    const message = createBaseNodeMetrics();
    message.qps = object.qps ?? 0;
    message.latencyMs = object.latencyMs ?? 0;
    message.errorRate = object.errorRate ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
