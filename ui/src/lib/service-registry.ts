import { ToolDefinition } from "@proto/config/v1/tool";

/**
 * ServiceRegistry manages the mapping between services and tools.
 * It provides a central place to lookup which tools belong to which service.
 */
export const SERVICE_REGISTRY = {
    // ...
};
