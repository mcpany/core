--- Makefile
+++ Makefile
@@ -38,15 +38,15 @@

 test: gen
	$(MAKE) test-proto
	$(BAZELISK) test //server/... //ui/...
	$(MAKE) -C k8s test

 docker-build-all:
	# Build OCI images via Bazel (hermetic, Dockerfile-free where possible).
	$(BAZELISK) run //server/cmd/server:server_tarball
	$(BAZELISK) run //ui:ui_tarball
	# The http-echo-server and other auxiliary images still use Docker:
	$(MAKE) -C server docker-build-http-echo

 build-docker:
	# Load the Bazel-built server OCI image into Docker.
	$(BAZELISK) run //server/cmd/server:server_tarball

 test-proto:
	@echo "Running proto tests..."
	@echo "GO_CMD is $(GO_CMD) and INSIDE_DOCKER_CONTAINER is $(INSIDE_DOCKER_CONTAINER)"
	@./build/env/bin/bazelisk test //proto/...

 k8s-e2e:
	@export PATH=$(TOOL_INSTALL_DIR):$$PATH; $(MAKE) -C k8s test

 k8s-test: k8s-e2e

 lint:
-	$(BAZELISK) run //:lint
+	export GOGC=off && export GOMAXPROCS=2 && $(BAZELISK) run //:lint
